# 阶段五 第 63 轮（开发推进）：并发修复后的并行验证与队列/游标继续

## 登记（运行前写入，2026-09-13，Asia/Shanghai）

- 目的：第 62 轮的并发写入缺陷已修复（`atomic.py` 唯一临时名 + `file_lock`）。本轮**仍按并行
  口径**运行，验证修复在真实多进程下不再崩溃、状态更新不互相覆盖；同时继续推进队列与游标。
- 追加：CN-08 第 62 轮进程崩溃（退出码 1）后未写出该轮运行指标与报告，本轮对 CN-08 做一次
  小预算补跑（≤4 请求），确认修复后的同一路径正常结束并补齐该项记录；若列表入口无新目标，
  只记录零新增，不重复采集已在交付中的文档。
- 窗口：`--start-date 2026-09-06`（包含式下界）。
- 预算：IN-02 ≤16/450s、CN-02 ≤16/400s、IN-05 ≤10/600s、IN-06 ≤6/240s、IN-10 ≤6/240s、
  IN-01 ≤6/240s、CN-08 ≤4/180s（外层各 +60s）。合计 ≤64。
- 规则同前（robots、0.5 req/s、并发 1、边界、Retry-After、不绕过、预算停止先保存进度）；
  7 个运行同时启动以复现并发写入场景。

复跑命令示例：

```
UV_CACHE_DIR=/tmp/uv-cache uv run --locked --no-python-downloads python -m crawler.cli collect \
  --config src/crawler/config/sources.yaml --source IN-02 \
  --start-date 2026-09-06 --max-items 7 --max-requests 16 --deadline-seconds 450 --json
```

## 结果

### 登记补充（重启说明，2026-09-13 22:2x）

- 首次以普通后台方式启动 7 个运行，进程在启动命令结束后被工具进程组回收（未产出运行汇总）：
  仅 CN-08 归档 1 个发现页原件（账本 `CN-08_20260913_0014`），其余为 robots 探测；
  未产生文档、失败或跳过判定。登记预算不变，重启后各来源仍按上述上限执行；
  回收前消耗的请求计入本轮，按“实际发出”口径统计。
- 重启改用持久会话内 `wait` 的启动方式，保持并发口径不变。
### 登记补充 2（CN-02 游标推进补跑 + 并发对账复核，2026-09-13 22:3x）

- 情况：本轮 CN-02 首次运行时漏传 `--keyword 印度`，`adapter.discovery=[search]` 的检索发现
  未触发（只消费队列 7 篇），游标仍为 `nPageIndex_=10`；其余 6 个运行已完成。
- 复核发现：并行运行下 `logs/metrics.json` 的按运行对账全部报差异——对账使用**全局**文件增量，
  把其他来源同时追加的账本/文档/块也算进了本次运行。已按来源归属修复
  （`output_stats(source_id=…)`，块行按 `doc_id` 前缀判定），并用两个来源并行运行复核。
- 追加运行：CN-02 带 `--keyword 印度` 补跑（≤14 请求，推进检索游标）；
  IN-05 附件续传（≤10 请求），两者**并行**启动以复核按来源对账。
- 规则同前（robots、0.5 req/s、并发 1、边界、Retry-After、不绕过、预算停止先保存进度）。
### 首批 7 个并行运行（22:25—22:26）

| 运行 | run_id | 请求 | 结果要点 | 结束方式 |
| --- | --- | --- | --- | --- |
| IN-02 | `IN-02_20260913T222557` | 16 | 从游标 `page=11` 取回第 11 页（发现 10 目标），处理 7 篇（28 块）+ 1 个新附件入队 → 游标 `page=12`；pending 62 | `request_budget` |
| CN-02 | `CN-02_20260913T222528` | 15 | 消费队列 7 篇（42 块）+ 7 个附件；发现方式 search 未触发（漏传 `--keyword`）→ 见补跑 | `ok`（发现 complete，0 目标） |
| IN-05 | `IN-05_20260913T222523` | 10 | 附件续传 9 个、pending 20→**12**；列表入口增量发现 53 个新目标入队 | `request_budget` |
| IN-06 | `IN-06_20260913T222528` | 6 | 处理 3 项（2 篇成文/71 块）；pending 1→2（新发现 3 个） | `request_budget` |
| IN-10 | `IN-10_20260913T222509` | 5 | 处理 2 篇（47 块）、6 条站外链接记跳过；pending 1→**0（队列清零）** | `ok` |
| IN-01 | `IN-01_20260913T222512` | 6 | 附件全部取完（10/10，pending **0**）；目标转 refresh 2 | `ok` |
| CN-08 | `CN-08_20260913T222504` | 4 | 补跑第 62 轮崩溃项：列表入口正常重取、0 新增、2 篇窗口外跳过；队列 0 | `ok` |

- 7 个运行**并行**完成，0 崩溃、0 失败（第 62 轮的 `FileNotFoundError` 未复现）。
- 复核：每个运行的 `coverage.queue_state` 与最终 `pending_items.json` 逐来源一致（无丢失更新）；
  `data/` 下无残留 `.tmp-*` 文件（唯一临时名按预期清理）。
- 对账修复复核见下节：首批 7 个运行使用修复前的全局增量对账，`reconciliation_ok=False` 属于
  并发口径下的误报（各来源计数与文件行数实际一致），修复后两个并行运行均 `ok=True`。

### 补跑 2 个并行运行（22:31）

| 运行 | run_id | 请求 | 结果要点 | 结束方式 |
| --- | --- | --- | --- | --- |
| CN-02（带 `--keyword 印度`） | `CN-02_20260913T223107` | 14 | 检索发现第 10 页 10 目标（上限停止），处理 5 篇（30 块）+ 新附件 → 游标 `nPageIndex_=11`、next 已就绪；pending 53 + 3 附件 | `request_budget` |
| IN-05 | `IN-05_20260913T223103` | 10 | 附件续传 8 个；pending 12→**4** | `request_budget` |

- 两个运行并行执行，`logs/metrics.json` 对账均为 `reconciliation_ok=True`（按来源归属修复生效）。
- 本轮合计约 90 个请求（首批 62 + 补跑 24 + 首次被回收启动的少量探测），0 失败；
  新增文档 **23** 篇（182→205）、块 **218**（4369→4587）、账本行 451→524；
  (`crawl check`) manifest=524、documents=205、blocks=4587、raw_files=449、失败账 3 行、追溯 100%。
- 游标/队列：IN-02 `page=12`；CN-02 `nPageIndex_=11`；IN-05 附件 4；IN-06 2；IN-10 0（清零）；
  IN-01 0（清零）；CN-08 0（增量无新增）。
