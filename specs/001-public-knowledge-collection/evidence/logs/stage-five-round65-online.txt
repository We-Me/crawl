# 阶段五 第 65 轮（开发推进）：提高单轮上限推进分页与附件覆盖

## 登记（运行前写入，2026-09-13，Asia/Shanghai）

- 目的：S5-03/S5-04 覆盖推进——把单轮目标数从 6—7 提到 14，减少每轮只推进一页的往返；
  继续 IN-02 `page=13`、CN-02 `nPageIndex_=12`（检索发现带 `--keyword 印度`）、
  IN-05 18 个待处理附件 + 51 个列表目标、IN-06 1 个目标；对已清零来源做增量重发现
  （IN-10、CN-04：按列表入口重取，比较是否出现新的窗口内目标）。
- 窗口：`--start-date 2026-09-06`（包含式下界）。
- 预算：IN-02 ≤28/600s、CN-02 ≤28/600s、IN-05 ≤12/600s、IN-06 ≤10/300s、
  IN-10 ≤6/240s、CN-04 ≤10/300s（外层各 +60s）。合计 ≤94。
- 规则同前（robots、0.5 req/s、并发 1、边界、Retry-After、不绕过、预算停止先保存进度）；
  6 个运行并行启动，按来源归属对账。

## 结果

| 运行 | run_id | 请求 | 结果要点 | 结束方式 |
| --- | --- | --- | --- | --- |
| IN-02 | `IN-02_20260913T223751` | 28 | 从游标 `page=13` 连续取回第 13—14 页（发现 19 目标），处理 12 篇（48 块）→ 游标 `page=15`；pending 71 + 1 附件 | `request_budget` |
| CN-02 | `CN-02_20260913T223751` | 28 | 从游标 `nPageIndex_=12` 取回第 12—13 页，处理 12 篇（72 块）→ 检索遍历到站点末页；pending 64 + 3 附件 | `request_budget` |
| IN-05 | `IN-05_20260913T223716` | 12 | 列表目标推进 2 篇（41 块），其页面新增 11 个附件候选；pending 49 目标 + 29 附件 | `request_budget` |
| IN-06 | `IN-06_20260913T223713` | 10 | 处理 6 项（4 篇成文/89 块）；pending 1 | `request_budget` |
| IN-10 | `IN-10_20260913T223726` | 6 | 增量重发现后处理 2 篇（59 块）、6 条站外链接记跳过；pending 1 | `request_budget` |
| CN-04 | `CN-04_20260913T223708` | 8 | 列表入口重发现（新增 1 目标、复查 5），处理 2 篇（36 块）、3 个窗口外跳过；pending 0 | `ok` |

- 6 个并行运行 0 失败、`reconciliation_ok=True`；队列计数与 `pending_items.json` 逐来源一致。
- 本轮合计 92 个请求、新增文档 **34** 篇（222→256）、块 **345**（4746→5091）、账本行 566→645；
  (`crawl check`) manifest=645、documents=256、blocks=5091、raw_files=559、失败账 3 行、追溯 100%。

### CN-02 检索遍历是否到末页（离线复核，对齐 S5-03）

- 巡检第 12 页原件：站点自身分页为 首页(1)/上一页(11)/下一页(13)/**尾页(13)**；
  第 13 页原件：首页(1)/上一页(12)/下一页(13)/尾页(13)，即“下一页”与“尾页”都指向自身，
  且第 13 页只有 **9** 条 `detail1.jsp` 链接（<10，末页不满一页）。
- 与游标累计值一致：13 页 × 10 − 1 = **129** 个目标（`targets_found=129`）。
  据此判定“关键词 印度 的检索结果遍历完成（站点末页）”，而非仅凭没有下一页链接推断；
  该结论只覆盖该检索入口，不代表条约库全站。
