# 阶段五 第 70 轮（验证）：CN-02 检索入口增量核对

## 登记（运行前写入，2026-09-13，Asia/Shanghai）

- 目的：第 69 轮发现检索（search）入口未走增量核对（`start_url` 条件过宽，已修复并补用例）；
  本轮用同一命令验证 CN-02 关键词“印度”检索入口恢复 `completed` 后只核对第 1 页、
  以 `incremental_head_checked` 结束，并继续消费队列（pending 27 目标 + 2 附件）。
- 窗口：`--start-date 2026-09-06`（包含式下界）。
- 预算：CN-02 ≤30/600s（外层 +120s），合计 ≤30。
- 规则同前（robots、0.5 req/s、并发 1、边界、Retry-After、不绕过）。

## 结果

- 补跑（同目的，追加预算 ≤30，合计 ≤60）：第一次补跑（30 请求）发现 CN-02 游标仍是 `active`
  （第 69 轮在检索修复前的代码路径上再次把恢复的游标改写为 active 并重取 2 页，`queue added=0`），
  因此增量核对未触发；按同一证据再次恢复 `completed` 后重跑验证。
| 尝试 | run_id | 请求 | 结果要点 | 结束方式 |
| --- | --- | --- | --- | --- |
| 第 1 次 | `CN-02_20260913T235241` | 30 | 处理 12 篇（72 块）；检索重取 2 页（`queue added=0`）——游标被第 69 轮改回 `active`，未触发增量核对 | `ok` |
| 第 2 次（恢复游标后） | `CN-02_20260913T235815` | 30 | 处理 13 篇（78 块）；**检索按增量核对：只取第 1 页、0 新增**（`incremental_head_checked`，complete=true）；目标 pending 14→**1**、附件待处理 1 | `request_budget` |

- 本目的合计 **60 个请求**、新增文档 **25** 篇（12+13）、块 **150**；0 失败。
- 验证结论：检索入口（`start_url` 由模板渲染）与列表入口行为一致；已完成入口不再整入口重取，
  队列消费不受影响（pending 只剩 1 个主目标 + 1 个附件）。
