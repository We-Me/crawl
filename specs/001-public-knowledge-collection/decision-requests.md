# 阶段交付决策请求（NEXT-05 输入）

## 最新环境状态

2026-09-13 用户已提供目标 WSL 安装成功证据：/usr/bin/soffice，LibreOffice 24.2.7.2 420(Build:2)，uv run 下 find_soffice() 同样返回 /usr/bin/soffice。NEXT-04 更新为 READY_FOR_VALIDATION：组件缺失阻塞已解除，真实 DOC/XLS 转换与追溯仍待验证；第四阶段整体未完成。见 [WSL 组件就绪证据](evidence/next04-wsl-component-ready.md)。下文早期缺组件/权限记录按历史时点理解，不再作为等待安装的理由。

版本：0.2.0｜日期：2026-09-11｜状态：待业务决定（附下方可填写的确认栏）；工程人员不代替需求方确认。

本文件把完成阶段交付所缺的业务决定集中列出：每项给出可选方案、已具备的工程证据、决定后可立即推进的动作
与不决定的后果。候选处理与依据的完整登记见 [待决事项](clarifications.md)，任务依赖见 [实施任务](tasks.md)。
这些决定不阻塞已完成工作；NEXT-01/02 和 NEXT-03 工程试点已完成；NEXT-04 另有 Linux 组件权限阻塞，不能视为完成。NEXT-06—NEXT-08 已完成。工程交付清单和必要决定输入已完成；当前续作见 [当前范围与分块](scope-and-blocking.md)。

## 决定 1（Q01）：采集交付与分块含义

2026-09-13 根据用户近期“不需要完成 RAG，最多做到语篇分块”的补充，本轮计划不包含向量化、索引、检索或问答；不再请求是否开发 RAG。原有 A/B/C 方案保留在 Git 历史，不作为当前待选项。

剩余需要明确：语篇分块是现有原始结构块，还是允许跨段组合的独立派生单元？前者复用当前 blocks，后者先冻结规则及与原块的追溯关系再开发。两者都不包含 RAG。详细边界见 [当前范围与分块](scope-and-blocking.md)。这不代替质量、来源、契约和发布方式的决定。

## 决定 2（Q11）：质量阈值与样本分母

- 追溯：沿用原文口径「documents/blocks 100% 可追溯」（当前工程已按此校验，最近一次真实试点 1/1、10/10）；
- 关键词验证：每来源至少 10 个测试词的样本与歧义判定规则；
- 其余待定：抽样量、召回分母、OCR 准确率、近似重复阈值、缺陷分级。

影响：AT-014（去重与来源关系）与 AT-024（内容与结构质量）目前标记 blocked，需要本决定才能转为正式判定；
T019 报告的 22 passed / 2 blocked 也由此收口。决定后可只补受影响用例，不重做既有夹具验证。

## 决定 3（Q12）：两套发现流程与站点规则落地方式

- 选项 A：按站点分别实现 S1/S2 顺序中适用的部分，保留实际策略记录（与 T026 机制一致）；
- 选项 B：统一固定一种顺序，站点差异只体现在配置开关；
- 具体待定项：域名别名（如 CN-01 旧域、IN-03 的 `indiacode.gov.in`）、栏目级规则取值、robots 不可用（CN-05/06/07 及 IN-04/07/08/09）的处理与申请方式。

## 决定 4（Q13）：启用来源、时间范围与运行参数

- 首批来源名单（含是否采纳 [CN-08 试点卡](pilot-cn08.md) 的参数：0.5 req/s、并发 1、无重试、仅当前栏目）；
- 历史起止（`2026-09-07` 是原文登记入口日期，不是现场核验结果）；
- 更新周期与调度方式、每域速率/并发上限、资源预算、交付统计分母。

影响：真实来源采集与 AT-001 等正式验收；工程侧可在决定后按站补 10 词/歧义验证。

## 决定 5（T002 数据契约冻结）

需要确认或修订的候选语义：抓取到文档的关联（Q03）、哈希/引用的统一含义（Q04）、原件身份与物理去重（Q05）、
元数据缺失口径（Q06）、附件关系（Q07）、结构数据与 full_text（Q08）、ID/顺序/枚举（Q09）；来源字段继承（Q10）只处理当前采集真正涉及的部分，领域继承不作为未选范围的前置；
失败、部分成功和恢复对账（Q15）。当前契约实现与样例以候选形式记录在 [数据契约](contracts/README.md)，已执行夹具和正式业务用例分别记录；不能声称全部 37 项业务用例已执行或通过。

影响：契约冻结后才可按正式基线判定 AT-008/AT-009/AT-021/AT-022；冻结时同步更新 schema、样例与受影响用例。

## 决定 6（Q14 剩余）：成果仓库与发布策略

代码仓库组织、交付包发布与运维交接方式仍 OPEN；用户的 src 布局与运行目录部分已确定，不影响当前命令面。

## 已可独立推进（不重复已完成工作）

按 [阶段四计划](stage-four.md) 完成 NEXT-09 交付清单（[delivery-inventory.md](delivery-inventory.md)）和 NEXT-05A 决策输入（本文件确认栏）；不重复已完成的 NEXT-06—NEXT-08。LibreOffice 权限改变后恢复 NEXT-04。没有具体缺陷或交付物时，不默认开展环境核对、故障演练或扩大来源选择器准备。

当前可一次确认采集阶段范围、相关候选契约、CN-08 正式范围及 Q11 质量口径；未选领域决定继续保留。重复测试不能替代这些决定，未收到确认时不得默认为同意。

## 确认栏（供需求方填写）

填写方式：对每项决定给出所选选项或修订意见，注明确认日期；决定可分批给出，未填写的项保持待定。
工程侧收到确认后按「决定后的动作」立即推进，不再重新生成同类决策文档。选项与依据的完整说明见上文各决定小节；
空白单元格由需求方填写，不由工程人员代填。

| 决定 | 选项（见上文章节） | 建议及依据 | 用户结论 | 确认日期 | 决定后的动作 | 受影响任务 |
| --- | --- | --- | --- | --- | --- | --- |
| 1（Q01） | 原始结构块／跨段组合（待明确） | 当前沿用原始 blocks；跨段方案需独立规则与追溯 | 本轮计划不做 RAG；分块含义待回答 | 2026-09-13 范围记录，分块确认待定 | NEXT-10 澄清后复用现有实现或形成明确变更 | T001/T002/T010/T027；T025 当前范围外 |
| 2（Q11） | 追溯口径沿用原文 100%；补关键词 10 词样本、歧义规则、抽样量、召回分母、OCR 准确率、近似阈值、缺陷分级 | 沿用「documents/blocks 100% 可追溯」；其余阈值需业务给口径 | 待填 | 待填 | 解除 AT-014/AT-024 blocked，收口 T019 报告的 2 项 blocked | T014、T019（AT-014/AT-024） |
| 3（Q12） | A 按站实现 S1/S2 适用部分／B 统一顺序加配置开关；含域名别名、栏目级取值、robots 不可用处理 | A；与已实现的 T026 逐来源适配机制一致 | 待填 | 待填 | 固化各站适配规则取值并补逐站证据 | T026 |
| 4（Q13） | 首批来源名单（含是否采纳 CN-08 试点参数）、历史起止、更新周期与调度、每域速率/并发上限、交付统计分母 | 试点参数取 0.5 req/s、并发 1、无重试、仅当前栏目 | 待填 | 待填 | 启用所选来源并做正式采集验收 | T015、T026、正式验收 |
| 5（T002） | 确认或修订候选契约语义：Q03—Q09、Q10 当前采集部分、Q15 | 候选实现与样例已随仓库登记，待冻认为基线 | 待填 | 待填 | 冻结契约，同步 schema、样例与受影响用例 | T002、AT-008/AT-009/AT-021/AT-022 |
| 6（Q14） | 代码仓库组织、交付包发布与运维交接方式 | 用户的 src 布局与运行目录部分已确定，不影响当前命令面 | 待填 | 待填 | 按确认方式组织交付与运维交接 | T027 交接方式 |

本确认栏只汇总上文已有选项与依据，不新增候选、不改变各决定内容；填写后的结论以本表与上文共同为准。

## 当前确认状态

Q01 已按用户补充记录本轮不做 RAG 的范围边界，分块含义仍未确认；其余确认栏未填写的项仍待决定，不能据此自动批准。NEXT-05A 的输入准备已完成
（本表 + 上文选项与依据），后续直接引用本文件，不再重复生成决策文档。NEXT-05B/C 的正式验收以实际确认记录为前置，
不能根据工程通过自动关闭 Q 项。工程交付清单见 [delivery-inventory.md](delivery-inventory.md)。
