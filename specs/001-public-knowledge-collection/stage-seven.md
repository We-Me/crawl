# 阶段七：原型交付收尾（当前唯一执行入口）

更新：2026-09-14，c752807 后审查的三项代码缺口（P7-01—P7-03）已修复、验证并交付原型（实现提交 `e4e5c39`）。用户最新决定：时间紧迫，优先得到原型；保留已有阶段成果，只处理已确认代码问题，外部访问、代理、许可和缺失数据说明清楚后交由人工补齐，不继续压在开发上。无需重新确认该取舍。

**当前状态：三项代码缺口已按本轮顺序修复，定向验证、受影响闭环与一次候选回归通过；阶段七采集原型退出条件已满足，本轮开发结束，转入实际问题驱动维护。** 不自动开启阶段八，不重做前轮 S7-01/02/03；实现提交 `e4e5c39`，证据见 [原型收尾证据](evidence/stage-seven-prototype.md)（含提交、命令与结果、人工清单链接）。原阶段七计划与完成记录见 [历史说明](stage-seven-history.md)，原日志和 [交付证据](evidence/stage-seven-delivery.md) 按原提交适用。

## 本轮仅做的代码工作

按 P7-01 → P7-02 → P7-03 推进，均为阶段七后审查收尾子项，沿用原 T006/T015/T016/T019/T027，不新增业务范围。

| 子项 | 已确认问题 | 最小修复与定向验收 | 当前状态 |
| --- | --- | --- | --- |
| P7-01 附件与正文恢复隔离 | `_same_recovery_object` 把相同母文档 ID 的附件当正文片段；附件恢复可把仍 partial 的母页标 processed 并清空 continuation | 附件只处置自身；只有可证明属于该母页正文分页/API 的任务才能走母页续接。不能只用 doc_id 判断对象类型，身份不足时明确转人工。验证母页仍待续、附件恢复成功的组合，母页状态和进度不变；原正文片段恢复用例复用 | 已修复（`e4e5c39`；证据 §1：4 项定向通过，含 2 项原片段用例复用） |
| P7-02 人工处置与执行状态一致 | `resolve --action skip` 仅关闭失败账，队列仍 failed，随后 check 从通过变失败 | 复用统一身份处置逻辑，协调账本与队列；考虑同对象其他开放阶段，不能一条处置关闭全部失败。manual_review 保持可见并防止未获重新处置的对象被自动补抓；重复操作和写入失败需明确结果，可恢复，不声称未完成处置已成功。验证真实 failed 队列对象 skip 后对账一致，其他开放阶段和对象不误关闭 | 已修复（`e4e5c39`；证据 §2：8 项定向通过） |
| P7-03 空身份可选中 | 同 URL 同时有带日期/不带日期或带 doc_id/不带 doc_id 记录时，find_latest 无法选中空值身份 | 区分参数未指定与显式空值，或采用稳定身份 ID；选最小兼容接口，同步帮助与使用示例。验证混合身份中仅处置指定空值记录，不改其他记录。不要要求操作者手改 JSONL | 已修复（`e4e5c39`；证据 §3：2 项定向通过，帮助与错误提示含空值示例） |

上述问题已有隔离复现，前两项影响错误成功/错误处置，不能只记账后忽略。第三项是人工补齐所需的可操作性缺口。本轮只补相应测试，不进行新的全仓审计或预设更多极端组合；如果新发现会直接破坏原件/追溯/状态真实性，做最小修复或可靠隔离，其余登记维护即可。

`failures --summary` 的来源过滤只作用于失败账、队列和 partial 仍为全数据根统计：登记为非阻断局限，使用说明明确混合统计范围；不将其汇总数作为单来源完整性证据，本轮不强制实现统一筛选（保留）。

## 外部问题转人工补齐

本轮不主动联网核验，不再调查代理/CA/DNS、申请网站许可、替换站点入口、重试大附件或扩充来源数据。已有守规测试授权保留，只有用户另行要求外部复核才启用；开发使用现有原件、离线夹具和本机回环站点。目标仍登记 18 来源，受限和缺失须可见，不虚构成功。

[人工补齐清单](manual-follow-up.md) 是当前外部问题的唯一交接清单。每项列来源/对象、已有错误或证据、缺失影响、人工操作、必要条件与补齐后核验。没有新证据不重复诊断；尚未补齐不阻断原型。历史数据只读，不为演示清空错误账、重置队列或批量改写原件。

## 原型范围与验证预算

保留采集、raw/manifest、标准化、最小结构分块、日期参数、增量/队列及现有错误查询处置。复用 Python 3.9.25、uv、镜像和锁定依赖，目标 Linux/WSL 环境已有验证；不重装 LibreOffice、不引入框架/数据库/控制台。不做 RAG、高级解析质量优化、持续采集、生产部署或发布平台。

每个代码修复完成后执行对应定向验证；共享恢复/账本修改稳定后，复用已有闭环工具做一次受影响流程验证与必要的一次候选回归。通过后停止；已有无关证据直接引用，不反复跑全量或追求测试数增长。环境缺依赖先记录准确原因及目标环境运行办法，不因本机缺包升级 Python/全量换依赖，也不调查与代码验证无关的站点问题。实际未运行检查如实记录，不能引用旧结果冒充本轮通过。

## 原型退出条件

- [x] 三项代码缺口均完成最小修复或具备等效的可靠处置，有对应定向证据；无已知未控制的跨对象误关闭或错误成功——定向 4/8/2 项通过，全量 523 passed，见 [证据 §1—§4](evidence/stage-seven-prototype.md)。
- [x] 现有正常采集及受影响的恢复/人工处置可用；原件、manifest 和已输出文档/块可追溯，必要检查通过——闭环扩展步骤 3d—3i、最终 `check ok=True`，见 [闭环记录](evidence/logs/stage-seven-prototype-loop.txt)。
- [x] 运行说明给出实际可用的原型启动、错误查询和人工处置示例；人工补齐清单可执行，未验证或受限来源如实标识——见 [runbook](runbook.md)、[quickstart](quickstart.md) 与 [人工补齐清单](manual-follow-up.md)。

三项满足即交付“阶段七采集原型”，停止本轮开发（2026-09-14 已满足，转为实际问题驱动维护）。允许开放外部错误、partial 数据、受限来源与非阻断局限；不要求全部站点可访问、窗口数据完整、错误账清零或生产就绪。不以 T019/T026/T027 的全业务验收未全勾选延长原型开发，也不将其自动勾选。

实施完成后已新增 [evidence/stage-seven-prototype.md](evidence/stage-seven-prototype.md)，记录实际提交、三项结果、验证命令/结果和人工清单链接；本页及根说明、AGENTS、quickstart/runbook、任务和交付清单的当前状态已同步。旧阶段成果保留在历史记录中，不再次复制旧待办为当前入口。
