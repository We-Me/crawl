# 当前范围与分块边界（阶段四复核后的续作）

## 最新环境状态

2026-09-13 用户已提供目标 WSL 安装成功证据：/usr/bin/soffice，LibreOffice 24.2.7.2 420(Build:2)，uv run 下 find_soffice() 同样返回 /usr/bin/soffice。NEXT-04 更新为 READY_FOR_VALIDATION：组件缺失阻塞已解除，真实 DOC/XLS 转换与追溯仍待验证；第四阶段整体未完成。见 [WSL 组件就绪证据](evidence/next04-wsl-component-ready.md)。下文早期缺组件/权限记录按历史时点理解，不再作为等待安装的理由。

## 阶段总体状态（2026-09-13 补充）

第四阶段整体尚未完成，当前仅 NEXT-09 工程交接清单和 NEXT-05A 决策输入准备已完成。NEXT-04 已因用户提供的 WSL 安装证据转为 READY_FOR_VALIDATION；真实 DOC/XLS 成功转换尚未验证。本轮仅记录用户证据，未重跑目标 WSL 命令；模拟转换通过不能代替真实文件验收。T012/T019/T026/T027 保持部分完成，正式业务待决与环境阻塞分别记录。

日期：2026-09-13。复核基线：`e9e522c`；源码仍为 `d39bdc0` 的工程版本。当前请求为检查与修改后续规格，本轮不开发分块代码、不做真实采集、不复跑业务测试。

## 用户补充与未决边界

依据用户最近提出“当前项目好像不需要完成 RAG 部分，最多做到语篇分块”，本轮工程计划按采集、清洗标准化、分块和可追溯交付收敛，不安排向量化、向量库、索引、检索、重排或答案生成。该记录来自用户补充，不改写四份输入中原本存在的 RAG 要求，也不声称所有业务决定已冻结。

已有 blocks 是原始结构块：标题、段落、列表、表格、条款等，正文顺序和原件关系保留。“语篇分块”是否指这些结构块，或要求按语义完整性跨段合并，用户尚未回答；不能把“最多做到”当作已要求新增语义合并模块，也不能把现有结构分块当作语义分块已完成。

| 项目 | 当前实施边界 |
| --- | --- |
| 采集、原件、manifest、documents、blocks、失败账、日志、追溯 | 保留现有实现与正式验收要求 |
| 原始结构分块 | 已有实现；沿用原规则，不新增长度截断、重叠或语义合并 |
| 跨段语篇分块 | 仅作为待明确候选；未确认前不创建新业务契约或代码 |
| RAG 及索引/检索/问答/边缘检索部署 | 当前实施范围外；T025 不作为本轮交付阻塞，不标为已完成 |
| 七类领域对象 T020—T024 | 未选条件范围，继续保留原需求，不因采集或分块自动纳入。来源分类/等级等当前采集确需元数据照原契约维护 |
| 18 个来源正式接入 | 仍按 Q12/Q13 选定范围逐站落实，不因排除 RAG 自动完成 |

Q01 记录范围部分明确、分块语义等剩余 OPEN；Q17 保留原问题但本轮范围外，不再请求检索模型/硬件参数作为采集交付前置。FR-020 的采集阶段禁令继续适用。未来如明确选择跨段语义合并，需要单独记录用户范围修订，不能直接覆盖原始 blocks 或借用 RAG 实现。

## 第四阶段检查结论

NEXT-09 的交付清单、NEXT-05A 确认栏、limitations-report.md 已提交，不重新生成同类材料。Git 中 `d39bdc0` 至 `e9e522c` 的 src/tests/tools/pyproject.toml/uv.lock 无变更；345 passed 仍为阶段三历史证据，不是本轮执行结果。

delivery-inventory.md 的源码与文档提交基线已标明；源码外随包契约证据应引用 next08-installed-wheel.txt。当前 Windows 副本没有 dist/，历史 Linux 产物哈希仍可保留，但不能声称当前可下载或已交付。数据根大小、临时目录与文件数均按历史执行时点理解，不为更新文档重建产物或复制真实数据。

## 下一轮有限任务

| 工作项 | 状态 | 成果与停止条件 |
| --- | --- | --- |
| NEXT-10（T001/T002/T010/T027 的分块澄清子项） | READY，规格准备；实施选择待回答 | 优先复用 data-model.md 与已有样例，在原文档内提供一组短示例，比较“保留原结构块”和“语篇完整性跨段组合”；列明差异并请求一次分块含义确认。已有材料足以回答时直接引用，不再扩写新的文档包；不引入模型、分块库或检索组件 |
| NEXT-05B | WAITING_DECISION，正式采集验收 | 只请求当前采集范围相关的契约、Q11 质量口径与 Q12/Q13 来源决定，不把 RAG 或未选领域问题作为门槛。决定后有限验收，沿用 DEV-012 预算；不能直接把历史试点改名为正式验收 |
| NEXT-04 | READY_FOR_VALIDATION | 用户已在目标 WSL 安装组件并验证发现路径；立即恢复真实 DOC/XLS 转换及追溯验证，不重复安装或探测 sudo |
| NEXT-05C | 有条件收口 | 采集及已明确分块范围验收、交接方式具备后完成交付；RAG 未开发不再作为本阶段缺口。仍未通过的采集能力必须列明，不能降低标准来勾选完成 |

NEXT-10 只为消除新提出的分块歧义，属于既有任务子项，不新增第 28 项 T 任务。原始 37 条需求/AT 继续保留追踪，当前适用性单独注明；历史机器报告不因范围讨论重新生成。若确认只需原始结构块，直接复用实现并验收，禁止为了“下一阶段”另造分块功能。

若确认跨段组合，先定义独立派生单元：唯一 ID、doc_id、有序 block_ids、章节与原件定位、规则版本；文本忠实保留，不生成摘要或改写事实，不跨文档/语言/来源版本合并。表格、列表、条款、标题上下文及超长段落如何处理必须明确；长度、重叠和合并规则不得从测试常量推定。以上只是候选设计原则，具体文件名/字段/阈值尚未冻结，不修改现有 JSON Schema。

## 验证与交接

本轮仅执行文档链接、编号、原件指纹及引用校验。下一轮也不以 pytest 起步：先完成有限澄清，并推进不受该问题影响的采集决定。若只剩用户决定或环境阻塞，提交一次明确输入清单后等待，不重新进行交接清单整理、全量回归或扩站。确认的业务调整再同步 spec、tasks、acceptance 和追踪记录；原业务输入与历史日志不改写。

## 环境问题的恢复与停止条件

当前已收到组件就绪证据，下一轮直接在同一 WSL 恢复验证，不再等待安装。以下为换机或组件再次不可用时的恢复预案，历史 [环境证据](evidence/logs/t012-libreoffice-env.txt) 仅作背景。需要恢复时登记所需输入：管理员安装可用 LibreOffice，或提供满足既有转换接口的可用转换器及其路径；保留 BLOCKED_ENV 并推进独立工作，不重复尝试 sudo、不通过提高 Python 版本或添加 PyPI 依赖处理系统组件缺失。

条件具备后只做一次针对性恢复验证：在目标 Linux 记录组件路径、版本和转换调用方式，用真实 DOC/XLS 各一份检查转换、解析结果及原件追溯，保留命令与输出证据。通过后仅关闭 NEXT-04 对应环境缺口；失败则记录具体原因，不宣称第四阶段完成。NEXT-05B/C 仍须满足相关业务决定及正式验收条件。


### 组件缺失时的安装预案（用户已完成安装，当前不重复执行）

首选由管理员在已有 Ubuntu 24.04 目标环境安装发行版维护的稳定组件。先核查当前配置的系统镜像与候选版本，不照抄历史版本号、不启用 backports 追新；PyPI 镜像与 apt 系统镜像分别管理。

```bash
# 管理员在目标 Ubuntu 主机执行；sudo 密码由管理员在终端输入
sudo apt-get update
apt-cache policy libreoffice-writer libreoffice-calc
sudo apt-get install --no-install-recommends libreoffice-writer libreoffice-calc

# 以实际运行爬虫的用户执行
command -v soffice libreoffice
soffice --version
# 在项目根目录，复用现有 uv 环境检查程序能否发现组件
uv run --locked --no-python-downloads python -c "from crawler.parser.legacy_parser import find_soffice; p=find_soffice(); print(p); assert p, 'LibreOffice not found in runtime PATH'"
```

若组件装在非标准目录，由管理员将包含 soffice 的目录加入实际运行用户的 PATH（服务部署时设置对应服务环境），随后执行上面的发现检查。当前代码只通过 PATH 查找 soffice/libreoffice，没有专用的 LibreOffice 路径环境变量，不虚构 .env 配置项。

若无法取得安装权限，可由管理员提供预装组件的目标 Linux 运行环境，或提供能满足 parse_legacy(converter=...) 接口的转换器。现有注入是 Python 函数接口，接收原始 bytes 与扩展名，返回转换后 bytes 与 .docx/.xlsx 扩展名；它不是现成 CLI 配置开关。采用替代转换器前需记录选型与接线变更并针对验证，不默认新增依赖或重构部署体系。

组件发现检查仅确认可执行文件可用。之后按 NEXT-04 使用真实 OLE2 DOC/XLS 各一份验证成功转换与解析，核对段落/表格/sheet 等已知内容和原件关联，记录版本、样本来源与哈希、调用结果及失败信息；不能将模拟转换器测试当作真实成功证据。安装失败时区分镜像/网络、包依赖或权限错误，有依据地修复该错误，不反复重试相同命令。
