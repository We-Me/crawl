# T026 第六轮：文章级发现与起始日期有限线上验证（登记与结果）

## 登记（运行前写入，2026-09-13，Asia/Shanghai）

- 目的：验证本轮新增能力在真实来源上的文章级行为——(1) 按站列表规则产出的文章目标，
  (2) 文章页 `publication_date` 解析，(3) `--start-date` 按内容发布日期的包含式下界
  （范围外只留原件与账本、不产文档），(4) `adapter.discovery` 未实现来源不发请求。
- 来源与顺序（逐站串行，一站失败不影响其他）：
  1. CN-08 新华网 `/xinhuashe/`（已配置 `list_link_selector`/`content_selector`，离线复算得 3 个文章目标）
  2. CN-01 外交部 `/eng/`（通用列表规则离线复算得 5 个 2026-09 文章目标）
  3. CN-04 中国政府网 `/zhengce/`（政策文件栏目入口，尚未有原件）
- 近期窗口：`--start-date 2026-09-06`（运行日前 7 天，包含式下界；仅本次工程验证，不写死为业务默认）。
- 请求预算（显式使用 CLI 预算参数，合计上限 22 个请求含 robots/重定向每跳/重试/分页/附件；
  超预算即停）：CN-08 ≤ 10、CN-01 ≤ 6、CN-04 ≤ 6；`--deadline-seconds` 90/60/60。
- 规则：HttpClient 默认 robots、限速 0.5 req/s、并发 1、重定向边界检查、重试上限 2；
  遇登录、验证码、明确拒绝或访问限制立即停止，不绕过；不连续重开相同运行规避预算。
- 数据：原件、账本、文档按 DEV-008 写入开发数据根 `data/`（gitignore，不进交付目录）。
- 运行命令（复跑用；需网络）：

```
uv run --locked --no-python-downloads python -m crawler.cli collect \
  --config src/crawler/config/sources.yaml --source CN-08 \
  --start-date 2026-09-06 --max-items 4 --max-requests 10 --deadline-seconds 90
uv run --locked --no-python-downloads python -m crawler.cli collect \
  --config src/crawler/config/sources.yaml --source CN-01 \
  --start-date 2026-09-06 --max-items 3 --max-requests 6 --deadline-seconds 60
uv run --locked --no-python-downloads python -m crawler.cli collect \
  --config src/crawler/config/sources.yaml --source CN-04 \
  --start-date 2026-09-06 --max-items 3 --max-requests 6 --deadline-seconds 60
```

## 结果

## 追加段（第七轮登记与结果，2026-09-13）

第六轮中 CN-01 两次运行（6 + 8 个请求）均因入口开销（robots + 2 个入口页 + 子域重定向/重试）
在取到文章前耗尽预算：第一次 status=stopped requests=6，第二次 status=stopped requests=8，
resources=0、documents=0、failures=0——按预算停止已如实记账，未冒充成功。

随后完成一项离线可验证的按站适配（不发网络）：CN-01 增加
`adapter.list_link_pattern`（仅保留 `/eng/xw/zyxw/<YYYYMM>/t<id>_<id>.html` 文章）与 `max_pages: 1`。
用已归档英文首页复算：7 个目标全部为 2026-09 文章，导航、站内搜索页与 cs.mfa.gov.cn 子域链接不再入选
（首次写成 `20\d{6}` 只匹配 8 位编号，复算得 zero_results，已修正为 `\d{6}` 后复算通过）。

第七轮登记：目的=验证 CN-01 文章级目标与文章页发布日期、CN-04 `/zhengce/` 栏目入口；
窗口同第六轮（`--start-date 2026-09-06`，含上界为运行日）；预算 CN-01 ≤ 8、CN-04 ≤ 6（合计 ≤ 14，
含 robots/重定向每跳/重试）；其余规则同第六轮。数据仍写开发数据根 `data/`。

```
uv run --locked --no-python-downloads python -m crawler.cli collect \
  --config src/crawler/config/sources.yaml --source CN-01 \
  --start-date 2026-09-06 --max-items 3 --max-requests 8 --deadline-seconds 60
uv run --locked --no-python-downloads python -m crawler.cli collect \
  --config src/crawler/config/sources.yaml --source CN-04 \
  --start-date 2026-09-06 --max-items 2 --max-requests 6 --deadline-seconds 60
```

## 第八轮登记与结果（2026-09-13）

第七轮结果摘要（详见下方输出）：

- CN-01：requests=6、resources=3、documents=3、blocks=94、failures=0、skipped=0。
  3 篇文章均解析出 `publication_date=2026-09-12`，日期判定全部 `in_window`（起始日 2026-09-06）。
  这是 `--start-date` 首次在真实文章页上按内容发布日期完成判定。
- CN-04：requests=5、resources=2、documents=2、blocks=374、failures=0。发现两个目标都是入口
  自身与其 Big5 镜像，**未得到政策文件**；账本显示 `https://www.gov.cn/zhengce/` 302 到
  `https://www.gov.cn/`（首页），说明该入口不是政策文件列表页。

第八轮登记：目的=核实 CN-04 政策文件库入口（搜索库入口 + 待验证的 “最新政策” 路径），
窗口同上（`--start-date 2026-09-06`），预算 ≤ 5 个请求（含 robots/重定向每跳/重试）、
`--deadline-seconds 45`，其余规则同前。

```
uv run --locked --no-python-downloads python -m crawler.cli collect \
  --config src/crawler/config/sources.yaml --source CN-04 \
  --entry-url "https://sousuo.www.gov.cn/zcwjk/policyDocumentLibrary?t=zhengcelibrary" \
  --entry-url "https://www.gov.cn/zhengce/zuixin.htm" \
  --start-date 2026-09-06 --max-items 2 --max-requests 5 --deadline-seconds 45
```

第八轮结果：

- 政策文件库搜索入口 `https://sousuo.www.gov.cn/zcwjk/policyDocumentLibrary`：robots.txt 命中
  `Disallow /`，发现状态 `access_restricted`，只取 robots.txt、未请求页面（按规则不绕过）。
- `https://www.gov.cn/zhengce/index.htm`（首页“最新政策”链接）：可达（无失败），但静态 HTML
  中按 `/zhengce/*content_<id>.htm` 规则未发现任何文档链接，发现状态 `zero_results`
  （该栏目疑似前端渲染；不引入浏览器兜底）。据此 CN-04 改用首页作入口：从已归档首页离线复算，
  同一规则得到 7 个政策文件/解读目标（`/zhengce/content/...`、`/zhengce/jiedu/tujie/...`）。

第九轮登记：目的=验证 CN-04 用首页入口 + 政策文件规则能否取到真实政策文件并解析发布日期；
窗口同上（`--start-date 2026-09-06`）；预算 ≤ 5 个请求（含 robots/重定向每跳/重试）、45 秒；其余同前。

```
uv run --locked --no-python-downloads python -m crawler.cli collect \
  --config src/crawler/config/sources.yaml --source CN-04 \
  --start-date 2026-09-06 --max-items 3 --max-requests 5 --deadline-seconds 45
```

## 第六至九轮结果汇总（2026-09-13，实测数据来自 `data/logs/metrics_history.jsonl`）

| 轮次 | 来源 | requests | resources | documents | blocks | failures | 结果 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 6 | CN-08 | 5 | 3 | 1 | 6 | 0 | 3 篇文章目标；1 篇 in_window（发布日期 2026-09-06，等于下界 → 收录）；2 篇 before_start_date（2026-09-05、2026-08-28）只留原件与账本、不产文档 |
| 6/7 | CN-01 | 6+8 | 0 | 0 | 0 | 0 | 两次均按预算停止（status=stopped、unprocessed=3），未产文档、未冒充成功 |
| 7 | CN-01 | 6 | 3 | 3 | 94 | 0 | 文章规则生效；3 篇文章日期 2026-09-12，全部 in_window |
| 7 | CN-04 | 5 | 2 | 2 | 374 | 0 | 入口 `/zhengce/` 302 到首页，取到首页与其 Big5 镜像；未得到政策文件 |
| 8 | CN-04 | 1 | 0 | 0 | 0 | 0 | 搜索库入口 robots `Disallow /` → access_restricted，未请求页面 |
| 8 | CN-04 | 2 | 0 | 0 | 0 | 0 | `/zhengce/index.htm` 可达但静态无文档链接 → zero_results（疑似前端渲染） |
| 9 | CN-04 | 5 | 3 | 3 | 54 | 0 | 首页入口 + `/zhengce/*content_<id>.htm` 规则取到 3 份政策文件/解读；当时日期为 date_unknown |

补充（第九轮后，离线修复并复算）：CN-04 政策文件页的首次发布日期在
`<meta name="firstpublishedtime" content="2026-09-11-17:00:00">`，解析器未覆盖该 meta 名。
已将 `firstpublishedtime` 加入 `META_DATE_KEYS`（夹具用例 + 已归档原件离线复算）：
`content_7080735.htm`→2026-09-11、`content_7080627.htm`→2026-09-10、`content_7080629.htm`→2026-09-10，
三份均判 in_window。第九轮已写的 document 仍是修复前结果（`metadata_missing=[publication_date]`，
未回填、不伪造），下次采集/重解析即用新规则。

本日线上请求合计：第六轮 5+6+8=19、第七轮 6+5=11、第八轮 1+2=5、第九轮 5 → **40 个请求**，
0 个失败；无登录、验证码或明确拒绝情形；所有停止均为预算停止或 robots 规则。

## 第十轮登记与结果（2026-09-13）

目的：IN-06（PIB）当前记为“未完成”（首页 6 KB 前端壳、0 个 `<a>`）。用最小预算核验其列表入口
（`allRel.aspx`、`PressReleasePage.aspx`）是否服务端渲染；若有可发现链接，则据实登记列表规则，
否则保留“未完成”并记录页面形态。窗口 `--start-date 2026-09-06`，预算 ≤ 6 个请求（含 robots/每跳/重试）、
45 秒，其余规则同前。

```
uv run --locked --no-python-downloads python -m crawler.cli collect \
  --config src/crawler/config/sources.yaml --source IN-06 \
  --entry-url "https://www.pib.gov.in/allRel.aspx" \
  --start-date 2026-09-06 --max-items 2 --max-requests 6 --deadline-seconds 45
```

第十轮结果（含一项行为修正）：

- 第一次尝试（0 个请求）暴露不一致：来源 `adapter.discovery=[]` 时，显式 `--entry-url` 也被
  `resolve_strategy` 判为未实现，与配置注释“显式入口可按用户意图运行通用实现”不符。
  已修正为：显式给出入口时运行通用实现，但发现结果注明“来源未声明该发现方式，本轮按显式入口运行通用实现
  （未计入已实现）”；未显式给出入口仍记 `not_implemented` 且不发请求。新增 2 项用例。
- 修正后：IN-06 `allRel.aspx` → 200 但重定向到 `index.aspx` 前端壳（归档 6,075 字节、0 个 `<a>`、0 块），
  发现状态 `ok`（2 个候选目标，带未声明标注）；requests=6 达预算停止（status=partial、未处理 1），
  已归档壳页原件保留。结论：PIB 列表入口仍为前端渲染，IN-06 保持“未完成”，需站点接口或等价入口。

本日线上请求合计：40 + 6 = **46 个**（0 失败）。

## 第十一轮登记与结果（2026-09-13）

目的：IN-01（MEA）记为“实现待验证”：通用规则只选中首页，栏目级链接规则未定。本轮以
`https://www.mea.gov.in/bilateral-documents` 栏目为入口，核验其服务端列表形态并取出 2 篇文档目标；
若链接形态稳定，则据实登记列表规则（事后用已归档栏目页离线复算验证）。
预算 ≤ 5 个请求（含 robots/每跳/重试）、45 秒；窗口 `--start-date 2026-09-06`；其余规则同前。

```
uv run --locked --no-python-downloads python -m crawler.cli collect \
  --config src/crawler/config/sources.yaml --source IN-01 \
  --entry-url "https://www.mea.gov.in/bilateral-documents" \
  --start-date 2026-09-06 --max-items 2 --max-requests 5 --deadline-seconds 45
```

## 第十二轮登记与结果（2026-09-13）

第十一轮结果：IN-01 以 `bilateral-documents`（无 `.htm`）为入口时 302 到首页；首页原件（81,745 字节）中
仅 2 条 `?dtl/<id>` 文档链接（其余为导航/`#`），说明 MEA 文档 URL 形态是
`https://www.mea.gov.in/<section>?dtl/<id>`。robots 因 TLS 断开重试 2 次后成功（无规则）。
requests=5 达预算停止（status=partial、未处理 2），已归档首页原件与 1 份文档（119 块）。

第十二轮登记：目的=核验 MEA 泰米尔文？否——核验 `bilateral-documents.htm` 栏目页是否为服务端文档列表，
并据其链接形态确认 IN-01/IN-02 的列表规则。预算 ≤ 6 个请求（含 robots 重试/每跳）、45 秒；
窗口 `--start-date 2026-09-06`；其余规则同前。

```
uv run --locked --no-python-downloads python -m crawler.cli collect \
  --config src/crawler/config/sources.yaml --source IN-01 \
  --entry-url "https://www.mea.gov.in/bilateral-documents.htm" \
  --start-date 2026-09-06 --max-items 2 --max-requests 6 --deadline-seconds 45
```

第十二轮结果：`bilateral-documents.htm` 同样只发现首页目标（referrer 为该栏目），并在附件
`images/pdf/22899_India_in_Business.pdf` 上因 45 秒截止停止（status=partial、requests=5、1 份文档 119 块）。
据已归档首页证据（文档 URL 形态 `/<section>?dtl/<id>`，如 `press-releases?dtl/34086/`）为 IN-01/IN-02 增加
`list_link_pattern: "mea\.gov\.in/[^"']*dtl/\d+"` 与 `max_pages: 1`（离线核对：命中 2 条文档链接、
不再命中首页/导航）。第十三轮据此复验栏目入口。

## 第十三轮登记与结果（2026-09-13）

目的：用新列表规则复验 MEA 栏目入口能否得到真实文档目标（而非首页/导航）。预算 ≤ 6 个请求
（含 robots 重试与每跳）、`--deadline-seconds 60`；窗口 `--start-date 2026-09-06`；其余同前。

```
uv run --locked --no-python-downloads python -m crawler.cli collect \
  --config src/crawler/config/sources.yaml --source IN-01 \
  --entry-url "https://www.mea.gov.in/bilateral-documents.htm" \
  --start-date 2026-09-06 --max-items 2 --max-requests 6 --deadline-seconds 60
```

第十三轮结果：`bilateral-documents.htm` 在新规则下 `zero_results`（requests=3，robots + 栏目页 + 无目标），
说明该栏目静态 HTML 中没有 `?dtl/<id>` 文档链接（栏目疑似前端渲染或使用了其它真实列表 URL）。
IN-01/IN-02 因此保持“实现待验证”：文档 URL 形态已确认（`/<section>?dtl/<id>`，来自已归档首页），
但稳定列表入口仍需后续核验；不引入浏览器兜底、不猜测接口。

本日线上请求合计：46 + 5（第十一轮）+ 5（第十二轮）+ 3（第十三轮）= **59 个**（0 失败、0 绕过）。

## 第十四轮登记与结果（2026-09-13）

背景：CN-02（treaty.mfa.gov.cn）入口页为前端渲染、无站内链接，发现阶段不归档页面，无法离线分析其
检索/详情形态。为此补充 `crawl collect --url URL`（`discovery_method=manual`，仍受访问边界、robots、
起止日期与预算约束；只给 --url 时不隐式跑来源入口），并新增 3 项用例（流水线 2、CLI 1）。

目的：以 manual 方式归档 CN-02 入口页原件，供离线分析其静态表单/接口线索，判断能否实现发现策略。
预算 ≤ 3 个请求（robots + 页面）、30 秒；窗口 `--start-date 2026-09-06`（该页无发布日期时按未知保留）；
其余规则同前。

```
uv run --locked --no-python-downloads python -m crawler.cli collect \
  --config src/crawler/config/sources.yaml --source CN-02 \
  --url "https://treaty.mfa.gov.cn/" --start-date 2026-09-06 \
  --max-items 1 --no-attachments --max-requests 3 --deadline-seconds 30
```

第十四轮结果：CN-02 入口原件仅 387 字节，是 JS 跳转壳：`window.location.href="web/index.jsp"`。
真实入口应为 `https://treaty.mfa.gov.cn/web/index.jsp`（HTTP 200、无 robots 规则、0 块、1 份文档、
`metadata_missing=[publication_date]`）。

## 第十五轮登记与结果（2026-09-13）

目的：归档 `https://treaty.mfa.gov.cn/web/index.jsp`（CN-02 真实入口），离线分析其检索/详情形态，
判断能否实现列表或搜索发现。预算 ≤ 3 个请求（robots + 页面）、30 秒；窗口 `--start-date 2026-09-06`；
其余同前。

```
uv run --locked --no-python-downloads python -m crawler.cli collect \
  --config src/crawler/config/sources.yaml --source CN-02 \
  --url "https://treaty.mfa.gov.cn/web/index.jsp" --start-date 2026-09-06 \
  --max-items 1 --no-attachments --max-requests 3 --deadline-seconds 30
```

第十五轮结果：`web/index.jsp`（7,296 字节、9 块）是真实入口，含 **POST** 表单 `action=list.jsp`
（字段 `chnltype_c=all`、`keywords`、`keywords_val`）与 `advanceSearch.jsp`（高级搜索）链接。
现有 SearchDiscovery 只支持 GET 模板，需先确认 `list.jsp` 是否接受 GET 并服务端渲染结果。

## 第十六轮登记与结果（2026-09-13）

目的：以 GET 方式取 `web/list.jsp?chnltype_c=all&keywords=印度`，离线判断是否服务端渲染条目列表、
详情链接形态（如 `detail1.jsp?objid=`）。预算 ≤ 3 个请求、30 秒；窗口 `--start-date 2026-09-06`；
其余同前。

```
uv run --locked --no-python-downloads python -m crawler.cli collect \
  --config src/crawler/config/sources.yaml --source CN-02 \
  --url "https://treaty.mfa.gov.cn/web/list.jsp?chnltype_c=all&keywords=%E5%8D%B0%E5%BA%A6" \
  --start-date 2026-09-06 --max-items 1 --no-attachments --max-requests 3 --deadline-seconds 30
```

第十六轮结果：`list.jsp` GET 可用且服务端渲染——归档原件（10,604 字节）含 10 条
`detail1.jsp?objid=<id>` 结果与 `?nPageIndex_=N&keywords=…` 翻页链接（管道按正文分页继续取了第 2 页，
在预算处停止）。据此登记 CN-02 发现方式：`search_url_template=list.jsp?...keywords={query}` +
`adapter.discovery: [search]` + `list_link_pattern="detail1\.jsp\?objid=\d+"` + `max_pages: 1`；
真实入口改为 `web/index.jsp`（站点根是 JS 跳转壳）。离线核对：规则在已归档结果页命中 10 个详情目标。

## 第十七轮登记与结果（2026-09-13）

目的：用站内检索发现实现并验证 CN-02 的文章级采集（关键词“印度”，最近窗口 `--start-date 2026-09-06`）。
预算 ≤ 6 个请求（robots + 检索页 + 至多 2 个详情 + 附件/每跳）、45 秒；其余规则同前。

```
uv run --locked --no-python-downloads python -m crawler.cli collect \
  --config src/crawler/config/sources.yaml --source CN-02 \
  --keyword "印度" --start-date 2026-09-06 --max-items 2 --max-requests 6 --deadline-seconds 45
```

第十七轮结果：检索发现生效（`search 策略=site_search 状态=ok 目标=10`），取到 1 篇条约详情
（`detail1.jsp?objid=1535419721879`，6 块）与 3 个 PDF 附件；requests=6 达预算停止（status=partial）。
详情页日期为条约“生效时间/签署时间”（2018.01.19、2009.07.01），**不是**页面发布日期，
按字段语义不写入 `publication_date`（保持 `metadata_missing=[publication_date]`、`date_unknown` 保留）。

## 第十八轮登记与结果（2026-09-13）

目的：以不下载附件的方式复跑 CN-02 检索通道，确认完整完成（status=ok）而非预算停止。
预算 ≤ 6 个请求（robots + 检索页 + 2 篇详情）、45 秒；`--no-attachments`；窗口同上。

```
uv run --locked --no-python-downloads python -m crawler.cli collect \
  --config src/crawler/config/sources.yaml --source CN-02 \
  --keyword "印度" --no-attachments --start-date 2026-09-06 \
  --max-items 2 --max-requests 6 --deadline-seconds 45
```

第十八轮结果：`status=ok`、requests=4、resources=2、documents=2、blocks=12、failures=0，
检索通道完整通过（2 篇条约详情；`--no-attachments` 下未取附件）。

本轮附带修复（由 `crawl check` 检出）：正文分页部分写 `discovery_method=pagination`，补抓重取写 `retry`，
两者不在 manifest 契约枚举内（既有实现行为，本次因归档了多部分正文才暴露）。处理：契约枚举收录
`pagination`/`retry` 并标注为候选扩展（待 Q12/Q15 冻结），同步随包副本；`crawl check` 复跑通过
（manifest=34、documents=28、blocks=1420、failures=2、raw_files=31、追溯 100%）。
新增 `crawl collect --url`（manual）能力与 3 项用例，文档与 data-model/structure-comparison 已同步。

本日线上请求合计：59 + 14（第十四至十八轮）= **73 个**（0 失败、0 绕过）。

## 第十九轮登记与结果（2026-09-13）

背景（离线修复）：IN-10 已归档 `Home.aspx` 原件含 4 h2/17 p/2 table/2,326 字符，但解析为 0 块——
原因是解析器把 `<form>` 整段丢弃，而 ASP.NET 把整页内容包在 form 内。已改为保留 form 内容、
只丢弃控件（input/select/option/textarea/button），新增用例 `test_content_inside_form_is_kept_but_controls_are_dropped`；
离线复算同一原件：块=33（8 heading + 25 paragraph），标题 `About SOI`（原 0 块 → `partial`）。

目的：以真实站点复跑 IN-10，确认列表发现 + 通用正文抽取可用（无需正文选择器）。
预算 ≤ 5 个请求、45 秒；窗口 `--start-date 2026-09-06`；其余同前。

```
uv run --locked --no-python-downloads python -m crawler.cli collect \
  --config src/crawler/config/sources.yaml --source IN-10 \
  --entry-url "https://onlinemaps.surveyofindia.gov.in/" \
  --start-date 2026-09-06 --max-items 1 --max-requests 5 --deadline-seconds 45
```

第十九轮结果：`status=ok`、requests=4、resources=2、documents=1、**blocks=66**、failures=0、skipped=6
（6 条为站外域跳过）。IN-10 的列表发现与通用正文抽取可用，无需正文选择器；
0 块问题确认为解析器丢弃 `<form>` 所致（已修复并以夹具 + 真实原件离线复算验证）。

本日线上请求合计：73 + 4（第十九轮）= **77 个**（0 失败、0 绕过）。

## 第二十轮登记与结果（2026-09-13）

目的：IN-05（MHA）记为“实现待验证”（首页可达、68 条站外链接被边界跳过、建议改用 Divisions/文稿栏目）。
先以 manual 方式归档首页原件，离线找出可用的站内栏目入口与链接形态，再实现列表规则。
预算 ≤ 3 个请求（robots + 首页）、30 秒；`--no-attachments`；窗口 `--start-date 2026-09-06`；其余同前。

```
uv run --locked --no-python-downloads python -m crawler.cli collect \
  --config src/crawler/config/sources.yaml --source IN-05 \
  --url "https://www.mha.gov.in/" --max-items 1 --no-attachments \
  --max-requests 3 --deadline-seconds 30
```

第二十轮结果：requests=3 全部消耗在 robots(1) + 首页重试（未归档、status=stopped），
说明该主机的页面获取需要更多重试预算；未记为失败（预算停止，未完成）。

附带修复：IN-10 第十九轮运行出现“正文分页出现循环 url=.../Home.aspx#myCarousel”，
即只改锚点的 rel=next（轮播）被当成正文分页。已在解析层忽略“只改锚点”的下一页链接
（`_next_page_url`），新增用例；夹具与离线复算验证通过。

## 第二十一轮登记与结果（2026-09-13）

目的：同第二十轮（归档 IN-05 首页原件、离线找可用栏目入口），提高请求预算以覆盖重试：
≤ 6 个请求、60 秒；`--no-attachments`；窗口 `--start-date 2026-09-06`；其余同前。

第九轮之后累计线上请求：77（截至第十九轮）；本轮计入。

第二十一轮结果：`status=ok`、requests=6、resources=1、documents=1、blocks=156——首页归档成功
（最终 URL `/hi.html`，站点默认印地语）。首页原件含 159 条站内链接：`/hi/divisionofmha/<名>`、
`/hi/division_of_mha/<名>`、`/hi/media/whats-new`、`/hi/commoncontent/press-release-*`、
`/sites/default/files/*.pdf`，并有语言切换 `English → http://www.mha.gov.in/en`。

## 第二十二轮登记与结果（2026-09-13）

目的：归档英文首页 `https://www.mha.gov.in/en`，据其站内链接确定 IN-05 的列表入口与规则。
预算 ≤ 4 个请求（robots + 重试/跳转）、45 秒；`--no-attachments`；窗口同上；其余同前。

第二十二轮结果：英文首页归档成功（`/en`，162 块，2 请求）。据其 160 条站内链接登记 IN-05 适配：
入口 `https://www.mha.gov.in/en`，`list_link_pattern` 限于 `/en/` 的文稿/通知/司局/媒体栏目
（离线核对命中 54 个目标），`max_pages: 1`；站点根 302 到印地语 `/hi.html` 已记录在 robots_policy。

## 第二十三轮登记与结果（2026-09-13）

目的：用新规则复跑 IN-05，确认英文文稿列表可用。预算 ≤ 6 个请求（robots + 入口 + 至多 2 目标 + 每跳）、
60 秒；`--no-attachments`；窗口 `--start-date 2026-09-06`；其余同前。

第二十三轮结果：`status=ok`、requests=4、resources=2、documents=2、blocks=125、failures=0。
IN-05 由“实现待验证”转为“已实现并验证”（英文栏目规则命中 2 个文稿页并产出文档）。

本日线上请求合计：73 + 4（第十九轮）= 77；此后 3（第二十轮）+ 6（第二十一轮）+ 2（第二十二轮）
+ 4（第二十三轮）= 15 → **92 个**（0 失败、0 绕过）。

## 第二十四轮登记与结果（2026-09-13）

背景：IN-01/IN-02 仍“实现待验证”——文档 URL 形态已确认（`/<section>?dtl/<id>`），但栏目入口
`bilateral-documents[.htm]` 静态无链接。本轮以 manual 归档另一候选栏目 `press-releases.htm`，
判断 MEA 是否有服务端渲染的文稿列表可作为入口。

目的：归档 `https://www.mea.gov.in/press-releases.htm` 并离线检查 `?dtl/` 链接。预算 ≤ 3 个请求、30 秒；
`--no-attachments`；窗口 `--start-date 2026-09-06`；其余同前。

第二十四轮结果：requests=3 全部用在 robots 与 `press-releases.htm` 的重试/跳转，未归档（status=stopped，
预算停止如实记录）。MEA 主机在当前网络下需要更多重试预算；IN-01/IN-02 保持“实现待验证”，
下一轮应先给足预算（≥8）或改用其 sitemap/检索接口，不猜测接口、不绕过。

本日线上请求合计：92 + 3 = **95 个**（0 失败、0 绕过；其中 2 次以预算停止结束并如实记录）。

## 第二十五轮登记与结果（2026-09-13）

目的：IN-01/IN-02 仍“实现待验证”。上一轮 MEA 页面在 ≤3 请求内未归档（重试耗尽），
本轮给足预算核验 `press-releases.htm` 文稿栏目能否产出 `?dtl/` 文档目标（规则已登记）。
预算 ≤ 10 个请求（含 robots 重试与每跳）、90 秒；`--no-attachments`；窗口 `--start-date 2026-09-06`。

```
uv run --locked --no-python-downloads python -m crawler.cli collect \
  --config src/crawler/config/sources.yaml --source IN-01 \
  --entry-url "https://www.mea.gov.in/press-releases.htm" \
  --start-date 2026-09-06 --max-items 2 --no-attachments \
  --max-requests 10 --deadline-seconds 90
```

第二十五轮结果：`press-releases.htm` 在新规则下 `zero_results`（requests=3、status=ok、0 目标）——
与 bilateral-documents 相同，MEA 栏目页静态 HTML 无 `?dtl/` 链接。改用**已核验的首页**作入口
（首页原件中可见 `press-releases?dtl/34086/` 等文稿链接），入口改为
`https://www.mea.gov.in/` + `bilateral-documents.htm`（后者保留为已核验可达入口）。

## 第二十六轮登记与结果（2026-09-13）

目的：以首页入口 + `?dtl` 详情规则验证 IN-01 文稿采集。预算 ≤ 8 个请求（robots + 2 入口 + 至多 2 目标 + 每跳）、
60 秒；`--no-attachments`；窗口 `--start-date 2026-09-06`。

```
uv run --locked --no-python-downloads python -m crawler.cli collect \
  --config src/crawler/config/sources.yaml --source IN-01 \
  --start-date 2026-09-06 --max-items 2 --no-attachments \
  --max-requests 8 --deadline-seconds 60
```

第二十六轮结果：`status=ok`、requests=5、resources=1、documents=1、blocks=29、failures=0、
status_counts.success=1。发现 `list_pagination` 命中 1 个目标：
`https://www.mea.gov.in/press-releases?dtl/41365`（`manifest.discovery_method=list`，入口为首页，
`bilateral-documents.htm` 本次未产出候选）。该文稿页无解析出的发布日期 → `date_unknown`，
按规则保留候选并写入文档（29 个结构块），未伪造日期。
IN-01 由“实现待验证”转为“已实现并验证”（首页入口 + 已核验 `?dtl` 详情规则产出真实文档）。

本日线上请求合计：95 + 3（第二十五轮）+ 5（第二十六轮）= **103 个**（0 失败、0 绕过；
其中 2 次以预算停止结束并如实记录）。

## 第二十七轮登记与结果（2026-09-13）

目的：IN-02 是唯一“实现待验证”来源。核验其条约列表入口
`https://www.mea.gov.in/treatylist-generic.htm` 的 list 发现能否产出 `?dtl/<id>` 目标（规则已登记）。
若 `zero_results`（IN-01 栏目页同款前端渲染），则用 `--url` 归档该列表页做离线链接分析，如实记录，
不猜测站点接口、不绕过。预算：两阶段合计 ≤ 8 个请求（robots 1 + 入口/URL + 重试/每跳）、90 秒；
`--no-attachments`；窗口 `--start-date 2026-09-06`。

```
uv run --locked --no-python-downloads python -m crawler.cli collect \
  --config src/crawler/config/sources.yaml --source IN-02 \
  --entry-url "https://www.mea.gov.in/treatylist-generic.htm" \
  --start-date 2026-09-06 --max-items 2 --no-attachments \
  --max-requests 5 --deadline-seconds 75
```


## 第二十八轮登记与结果（2026-09-13）

背景（第 27 轮结果，先记录）：`--entry-url treatylist-generic.htm` → `zero_results`（requests=3、0 目标）；
`--url` 归档列表页（requests=3、1 资源、20 块）离线分析：静态 HTML 无 `?dtl/<id>` 链接，条约列表由页面自身
JS 调用站点端点 `/FrontEnd/FetchTreatyListGenericLatest`（`GetTreatyDataGenericList`，返回 HTML 片段写入
`#DivTreatyListGeneric`）与 `/FrontEnd/GetSubjectListData`（科目列表）提供；`TreatyList?1` 分类页同样为前端壳
（requests=2、1 资源、16 块，仅导航/分类链接）。第 27 轮合计 8 个请求（= 登记上限）。

目的：以 manual `--url` 归档站点自身端点的一页响应，确认其响应形态与是否含 `?dtl/<id>` 详情链接；
据此决定是否把端点登记为 IN-02 入口（不猜测、不构造未公开接口）。预算 ≤ 4 个请求（robots + URL + 重试）、
60 秒；`--no-attachments`；窗口 `--start-date 2026-09-06`。

```
uv run --locked --no-python-downloads python -m crawler.cli collect \
  --config src/crawler/config/sources.yaml --source IN-02 \
  --url "https://www.mea.gov.in/FrontEnd/FetchTreatyListGenericLatest?page=1&PageSize=10&sortBy=sortby" \
  --start-date 2026-09-06 --no-attachments --max-requests 4 --deadline-seconds 60
```


第二十八轮结果：`status=ok`、requests=3、resources=2、documents=1、blocks=40、failures=0。
端点返回**真实 HTML 片段**：含 8 条条约详情链接，形态为 `/TreatyDetail?<id>`（如 `TreatyDetail?4896`），
以及分页链接 `?page=2..433`（共 433 页，站点默认 PageSize=10）。第二个资源是分页跟随得到的
`FetchTreatyListGenericLatest?page=5`（192 字节、0 链接，疑似缺参空响应，如实记录）。
结论：条约详情 URL 形态与 IN-01 的 `?dtl/<id>` 不同；登记 IN-02 端点入口并更新 `list_link_pattern` 为
`mea\.gov\.in/(TreatyDetail\?\d+|.*dtl/\d+)`（保留旧形态兼容）。

本日线上请求合计：103 + 8（第二十七轮）+ 3（第二十八轮）= **114 个**（0 失败、0 绕过）。

## 第二十九轮登记与结果（2026-09-13）

目的：IN-02 端到端核验——以站点自身条约列表端点为入口（`entry_urls` 已更新）、
新 `list_link_pattern` 命中 `/TreatyDetail?<id>`，采集详情页并核验日期判定与结构分块。
预算 ≤ 6 个请求（robots 1 + 入口 1 + 详情 ≤2 + 重试/每跳）、90 秒；`--no-attachments`；
窗口 `--start-date 2026-09-06`。

```
uv run --locked --no-python-downloads python -m crawler.cli collect \
  --config src/crawler/config/sources.yaml --source IN-02 \
  --start-date 2026-09-06 --max-items 2 --no-attachments \
  --max-requests 6 --deadline-seconds 90
```


第二十九轮结果：`status=ok`、requests=4、resources=2、documents=2、blocks=8、failures=0、
status_counts.success=2。发现 `list_pagination` 状态 `ok`、命中 2 个目标（端点为入口，规则命中
`/TreatyDetail?<id>`）。两篇条约详情均为真实内容（标题 + 元数据表：部委/条约类型/签署日期/生效日期/国家），
无发布日期 → 按规则 `date_unknown` 保留并写入文档（各 4 个结构块：2 heading + 1 段落标题 + 1 元数据表），
未伪造日期、未跳过。IN-02 由“实现待验证”转为**已实现并验证**：18 个来源中 8 个已实现并验证、
0 个待验证、9 个访问受限、1 个未完成（IN-06）。

本日线上请求合计：114 + 4（第二十九轮）= **118 个**（0 失败、0 绕过）。

## 第三十轮登记与结果（2026-09-13）

背景：IN-06 仍是唯一“未完成”来源。离线分析已归档壳页 `index.aspx?reg=48&lang=2`（6 KB、0 个 `<a>`）发现：
其脚本按 UA 跳转到 `indexd.aspx?reg=48&lang=2`（桌面）或 `indexm.aspx?reg=48&lang=2`（移动）——
这是此前未核验的真实入口，不是猜测接口。

目的：以 manual `--url` 归档桌面入口 `indexd.aspx?reg=48&lang=2`，离线判断是否为真实列表页
（含文稿链接及其 URL 形态），据此决定是否登记发现规则。预算 ≤ 4 个请求（robots 1 + URL + 重试/每跳）、
60 秒；`--no-attachments`；窗口 `--start-date 2026-09-06`。

```
uv run --locked --no-python-downloads python -m crawler.cli collect \
  --config src/crawler/config/sources.yaml --source IN-06 \
  --url "https://www.pib.gov.in/indexd.aspx?reg=48&lang=2" \
  --start-date 2026-09-06 --no-attachments --max-requests 4 --deadline-seconds 60
```


第三十轮结果：`status=ok`、requests=2、resources=1、documents=1、blocks=78、failures=0。
`indexd.aspx?reg=48&lang=2` 是**真实桌面列表页**（228 KB、164 个唯一链接）：含 15 条
`/PressReleaseDetail.aspx?PRID=<id>` 文稿链接、导航、PDF 等；robots.txt 0 组 0 规则。
据此登记 IN-06 发现规则：`entry_urls=[indexd 桌面入口]`、`discovery=[list]`、
`list_link_pattern=pib\.gov\.in/PressReleaseDetail\.aspx\?PRID=\d+`、`max_pages: 1`。

## 第三十一轮登记与结果（2026-09-13）

目的：IN-06 端到端核验——以 indexd 桌面入口运行 list 发现，采集 2 篇 PIB 文稿（PRID 详情），
核验日期判定与结构分块。预算 ≤ 6 个请求（robots 1 + 入口 1 + 详情 ≤2 + 重试/每跳）、90 秒；
`--no-attachments`；窗口 `--start-date 2026-09-06`。

```
uv run --locked --no-python-downloads python -m crawler.cli collect \
  --config src/crawler/config/sources.yaml --source IN-06 \
  --start-date 2026-09-06 --max-items 2 --no-attachments \
  --max-requests 6 --deadline-seconds 90
```


第三十一轮结果：`status=ok`、requests=6（=预算上限）、resources=2、documents=2、blocks=76、failures=0。
发现 `list_pagination` 状态 `ok`、2 个目标；文档写入成功、0 失败。**但内容质量两个问题如实记录**：
(1) `lang=2` 渲染的是**印地语**（列表页 `<html lang="en">` 但标题为印地语，文稿正文为印地语），
与来源配置 `language: en` 不符；(2) 详情页发布日期未解析出（正文标题含 `(12 सितंबर 2026)`、
页脚“अंतिम अद्यतन तिथि 13 Sep 2026”是站点更新时间非发布日期）→ `date_unknown`。

本日线上请求合计：118 + 2（第三十轮）+ 6（第三十一轮）= **126 个**（0 失败、0 绕过）。

## 第三十二轮登记与结果（2026-09-13）

目的：PIB 的 `lang` 参数疑似 1=英文、2=印地语。以 manual `--url` 归档 `indexd.aspx?reg=48&lang=1`，
离线确认语言与文稿链接形态，决定是否把入口改为英文版。预算 ≤ 4 个请求（robots 1 + URL + 重试/每跳）、
60 秒；`--no-attachments`；窗口 `--start-date 2026-09-06`。

```
uv run --locked --no-python-downloads python -m crawler.cli collect \
  --config src/crawler/config/sources.yaml --source IN-06 \
  --url "https://www.pib.gov.in/indexd.aspx?reg=48&lang=1" \
  --start-date 2026-09-06 --no-attachments --max-requests 4 --deadline-seconds 60
```


第三十二轮结果：`status=ok`、requests=2、resources=1、documents=1、blocks=83。
`indexd.aspx?reg=48&lang=1` 确认为**英文**版（Devanagari 字符 6 个 vs lang=2 版 9432 个；锚文本为
"Screen Reader Access" 等英文），文稿链接仍为相对形态 `/PressReleaseDetail.aspx?PRID=<id>`（不带参数）。

本日线上请求合计：126 + 2（第三十二轮）= **128 个**（0 失败、0 绕过）。

## 第三十三轮登记与结果（2026-09-13）

问题：详情页裸 URL 会被服务端重定向为 `&reg=48&lang=2`（默认印地语）——第三十一轮实测终址为
`PressReleaseDetail.aspx?PRID=2309469&reg=48&lang=2`、正文印地语。需要确认显式 `lang=1` 的详情 URL
是否返回英文（决定是否需要给列表目标追加语言参数的最小机制）。

目的：manual `--url` 归档 `PressReleaseDetail.aspx?PRID=2309469&reg=48&lang=1`，离线核对语言与日期。
预算 ≤ 4 个请求（robots 1 + URL + 重试/每跳）、60 秒；`--no-attachments`；窗口 `--start-date 2026-09-06`。

```
uv run --locked --no-python-downloads python -m crawler.cli collect \
  --config src/crawler/config/sources.yaml --source IN-06 \
  --url "https://www.pib.gov.in/PressReleaseDetail.aspx?PRID=2309469&reg=48&lang=1" \
  --start-date 2026-09-06 --no-attachments --max-requests 4 --deadline-seconds 60
```


第三十三轮结果：`status=ok`、requests=2、resources=1、documents=1、blocks=54。
显式 `lang=1` 详情页为**英文**（Devanagari 74 字符 vs 默认 7478）；但正文并不在详情页内，
详情页含 `<iframe src="PressReleasePage.aspx?PRID=2309469">`（站点自身 reader 页）；页脚
"Last Updated On: 13 Sep 2026" 是站点更新时间，非发布日。第三十一轮“详情文档”实际是外壳+侧栏，
需改采 reader 页。

本日线上请求合计：128 + 2（第三十三轮）= **130 个**（0 失败、0 绕过）。

## 第三十四轮登记与结果（2026-09-13）

目的：manual `--url` 归档 `PressReleasePage.aspx?PRID=2309469&reg=48&lang=1`，离线确认英文正文与
发布日期（如 "Posted on ... by PIB Delhi"）是否存在，决定 IN-06 的最小采集目标形态。
预算 ≤ 4 个请求（robots 1 + URL + 重试/每跳）、60 秒；`--no-attachments`；窗口 `--start-date 2026-09-06`。

```
uv run --locked --no-python-downloads python -m crawler.cli collect \
  --config src/crawler/config/sources.yaml --source IN-06 \
  --url "https://www.pib.gov.in/PressReleasePage.aspx?PRID=2309469&reg=48&lang=1" \
  --start-date 2026-09-06 --no-attachments --max-requests 4 --deadline-seconds 60
```


第三十四轮结果：`status=ok`、requests=2、resources=1、documents=1、blocks=26。
`PressReleasePage.aspx?PRID=2309469&reg=48&lang=1`（站点自身 reader 页）含真实文稿：
标题 `18th BRICS Summit Session ...`、发布日期 `प्रविष्टि तिथि: 12 SEP 2026 4:08PM by PIB Delhi`、
正文（该篇为印地语/英语混排，属文稿原貌）。结论：最小采集目标应为 reader 页，
但需确认 `lang`/`Lang` 参数对 reader 页语言变体的实际作用（详情页 og:url 仍指向 lang=2）。

本日线上请求合计：130 + 2（第三十四轮）= **132 个**（0 失败、0 绕过）。

## 第三十五轮登记与结果（2026-09-13）

目的：核对大小写参数 `Lang=1` 是否使 reader 页返回英文变体（站点部分自带链接使用 `Lang=2` 大写），
以确定 IN-06 目标 URL 的登记形态。预算 ≤ 4 个请求（robots 1 + URL + 重试/每跳）、60 秒；
`--no-attachments`；窗口 `--start-date 2026-09-06`。

```
uv run --locked --no-python-downloads python -m crawler.cli collect \
  --config src/crawler/config/sources.yaml --source IN-06 \
  --url "https://www.pib.gov.in/PressReleasePage.aspx?PRID=2309469&reg=48&Lang=1" \
  --start-date 2026-09-06 --no-attachments --max-requests 4 --deadline-seconds 60
```


第三十五轮结果：`Lang=1` 与 `lang=1` 返回同一页面（103,069 vs 103,070 字节、Devanagari 均为 8053）——
reader 页语言由文稿自身决定，URL 参数不改变变体；PRID=2309469 为印地语/英语混排文稿本身。

本日线上请求合计：132 + 2（第三十五轮）= **134 个**（0 失败、0 绕过）。

## 第三十六轮登记与结果（2026-09-13）

代码变更（本轮前置，含测试）：按站适配新增两个登记字段——
`list_link_rewrite`（列表目标等价形态改写，作用于绝对 URL，结果仍过边界检查）与
`date_selector`（发布日期元素选择器，未命中/不可验证回落 meta 与正文启发式，不补造日期）；
`date_utils` 支持月份缩写（`12 SEP 2026`）。IN-06 配置更新：
入口 `indexd.aspx?reg=48&lang=1`（英文）、目标改写为站点自身 reader 页、日期选择器 `#PrDateTime`。

目的：用新适配端到端核验 IN-06：列表发现 → 详情壳页改写为 reader 页 → 正文与发布日期解析
（窗口 `--start-date 2026-09-06`）。预算 ≤ 6 个请求（robots 1 + 入口 1 + reader ≤2 + 重试/每跳）、
90 秒；`--no-attachments`。

```
uv run --locked --no-python-downloads python -m crawler.cli collect \
  --config src/crawler/config/sources.yaml --source IN-06 \
  --start-date 2026-09-06 --max-items 2 --no-attachments \
  --max-requests 6 --deadline-seconds 90
```


第三十六轮结果：`status=ok`、requests=6（=预算上限）、resources=2、documents=2、blocks=38、failures=0。
发现 `list_pagination` 状态 `ok`、2 个目标；改写生效（原件 URL 均为站点自身 reader 页
`PressReleasePage.aspx?PRID=<id>`）；日期选择器生效：发布日期 `2026-09-12`、`2026-09-13`，
判定均为 `in_window`（包含式下界）。文档标题/正文为真实英文文稿（如《BRICS New Delhi Declaration》、
内政部长印地语日致辞预告），含 "Posted On ... by PIB Delhi" 与 Release ID。
**IN-06 由“未完成”转为“已实现并验证”**：18 来源状态 9 已实现并验证 / 0 待验证 / 9 访问受限 / 0 未完成。

本日线上请求合计：134 + 6（第三十六轮）= **140 个**（0 失败、0 绕过）。

## 第三十七轮登记与结果（2026-09-13）

目的：9 个“访问受限”来源中，7 个是 robots 获取失败（IN-04/IN-07/IN-08/IN-09）或 HTTP 508
（CN-05/CN-06/CN-07）。核对这些失败是否为暂时性网络问题（属 DEV-010 的“未解释失败”），
仅复核 robots.txt 可用性，不请求页面、不绕过：每个来源 1 次运行、≤2 个请求（robots + 重试）、
30 秒；不设起始日。

运行命令（逐来源）：
```
uv run --locked --no-python-downloads python -m crawler.cli collect \
  --config src/crawler/config/sources.yaml --source <ID> --max-requests 2 --deadline-seconds 30
```


第三十七轮结果（复核复现，未绕过、未请求页面）：

| 来源 | 站点 | 复核结果 |
| --- | --- | --- |
| IN-04 | egazette.gov.in | robots 仍失败：TLS `SSLZeroReturnError`（连接被重置）；curl 复核 exit=60、verify=20 → 本机信任库无法验证证书链 |
| IN-07 | censusindia.gov.in | robots 仍失败：`CERTIFICATE_VERIFY_FAILED: unable to get local issuer certificate`；curl 复核 exit=35、verify=1 |
| IN-08 | ladakh.gov.in | robots 仍失败：TLS `SSLZeroReturnError` |
| IN-09 | culture.sikkim.gov.in | robots 仍失败：TLS `SSLZeroReturnError` |
| CN-05 | www.xizang.gov.cn | robots 仍返回 508（保守拒绝，跳过首页） |
| CN-06 | wsb.xizang.gov.cn | robots 仍返回 508 |
| CN-07 | mw.xizang.gov.cn | robots 仍返回 508 |

结论：4 个 robots 获取失败与 3 个 508 均**稳定复现**，属站点/网络环境条件（TLS 信任链与 5xx），
非暂时性抖动；不通过关闭校验等方式绕过。维持“访问受限”，按 Q12 处理（需要外部决定/站点许可）。
CSV/日志与账本未新增原件、文档或失败记录（运行以跳过/受限结束，如实记录）。

本日线上请求合计：140 + 15（第三十七轮：8 次受限来源运行 + 4 次 curl 诊断 + 3 次西藏站点复核）
= **155 个**（0 失败、0 绕过）。

## 第三十八轮登记与结果（2026-09-13）

背景：离线扫描已归档原件发现 IN-02 条约详情页含条约 PDF 附件
（`/Portal/LegalTreatiesDoc/<id>_...pdf`，如 `4896_Indonesia_LOI_restoration_of_Prambanan_temple.pdf`），
此前各轮均 `--no-attachments`，附件获取未在该来源验证（CN-02 已验证 3 个 PDF）。

目的：IN-02 有限运行**启用附件**，验证条约 PDF 的原件留存、哈希、账本与追溯。
预算 ≤ 8 个请求（robots 1 + 入口 1 + 详情 2 + 附件 2 + 重试/每跳）、90 秒；窗口 `--start-date 2026-09-06`。

```
uv run --locked --no-python-downloads python -m crawler.cli collect \
  --config src/crawler/config/sources.yaml --source IN-02 \
  --start-date 2026-09-06 --max-items 2 --max-requests 8 --deadline-seconds 90
```


第三十八轮结果：`status=partial`（停止原因 `deadline`，未处理 2 项，退出码 3）、requests=4、resources=1、
documents=1、blocks=4、failures=0。MEA 主机单请求约 20s，90 秒内只完成 robots+入口+1 篇详情；
首选的站点级 footer PDF（`images/pdf/22899_India_in_Business.pdf`）在下载前被截止时间停止，
条约 PDF 未取到。离线核对详情页共 6 个附件链接：4 个是站点级 footer PDF（与条约无关）、
2 个是条约 PDF（`/Portal/LegalTreatiesDoc…` 与 `/Portal/LegalTreatiesDocHindi…`）。

代码变更（本轮前置）：按站适配新增 `attachment_pattern`（附件 URL 正则，入边界判定前过滤），
IN-02 登记 `mea\.gov\.in/Portal/LegalTreatiesDoc/`（英文条约 PDF；Hindi 目录与 footer PDF 不取）。
契约 `source-registry.schema.json` 同步；用例见 `tests/test_discover.py`、`tests/test_registry.py`。

本日线上请求合计：155 + 4（第三十八轮）= **159 个**（0 失败、0 绕过；1 次截止时间停止如实记录）。

## 第三十九轮登记与结果（2026-09-13）

目的：IN-02 附件获取端到端验证。第 38 轮已缓存列表第 1 页的详情（重新抓取会 304 跳过附件），
故入口改列表第 2 页（站点自身分页参数），取 2 篇新条约及其 PDF 附件；预算 ≤ 8 个请求
（robots 1 + 入口 1 + 详情 2 + 附件 2 + 重试/每跳）、240 秒（MEA 单请求约 20s）；窗口 `--start-date 2026-09-06`。

```
uv run --locked --no-python-downloads python -m crawler.cli collect \
  --config src/crawler/config/sources.yaml --source IN-02 \
  --entry-url "https://www.mea.gov.in/FrontEnd/FetchTreatyListGenericLatest?page=2&PageSize=10&sortBy=sortby" \
  --start-date 2026-09-06 --max-items 2 --max-requests 8 --deadline-seconds 240
```


第三十九轮结果：`status=ok`、requests=6、resources=4（2 详情页 + 2 条约 PDF）、documents=2、blocks=8、
failures=0。附件逐条核对：`4926_Solvakia_-_gainful_employment_eng.pdf`（497,201 字节）与
`4927_Slovakia_-_visa_exemption_diplomatic_eng.pdf`（363,767 字节），sha256 与账本一致、
`referrer_url` 指向母详情页、`content_type=application/pdf`；documents 的 `attachments` 字段引用附件
（经 attachment 契约校验通过）。`crawl check`：manifest=61、documents=51、blocks=2435、raw_files=58、
追溯 100%。IN-02 的入口、发现、日期（`date_unknown` 如实保留）、分块与**附件获取**均已落实并验证。

本日线上请求合计：159 + 6（第三十九轮）= **165 个**（0 失败、0 绕过；1 次截止时间停止如实记录）。

## 第四十轮登记与结果（2026-09-13）

背景：离线扫描 IN-05 归档原件，`/en/commoncontent/central-police-training` 页含 1 个 PDF 附件
（`/sites/default/files/Certificate-of-MHA.pdf`），是该来源发现规则范围内的页面；此前的线上运行均未启用附件。

目的：IN-05 附件获取核验（manual `--url` 指向规则内页面，取该 PDF）。预算 ≤ 4 个请求
（robots 1 + 页面 1 + 附件 1 + 重试）、60 秒；窗口 `--start-date 2026-09-06`。

```
uv run --locked --no-python-downloads python -m crawler.cli collect \
  --config src/crawler/config/sources.yaml --source IN-05 \
  --url "https://www.mha.gov.in/en/commoncontent/central-police-training" \
  --start-date 2026-09-06 --max-requests 4 --deadline-seconds 60
```


第四十轮结果：`status=ok`、requests=3、resources=2（页面 + `Certificate-of-MHA.pdf`）、documents=1、blocks=17、
failures=0。附件核对：604,919 字节、sha256 与账本一致、`referrer_url` 指向母页面、
documents.attachments 通过 attachment 契约校验。IN-05 的入口、发现、日期（文稿无日期 → `date_unknown`）、
分块与附件获取均已落实并验证。

本日线上请求合计：165 + 3（第四十轮）= **168 个**（0 失败、0 绕过；1 次截止时间停止如实记录）。

正文分页核查（离线，随本轮记录）：已归档的全部文章页未见 `rel=next` 或页码链接（皆单页正文），
仅 IN-02 列表片段含列表级 `?page=N` 链接（`max_pages: 1` 限幅，第 28 轮已记录）；
按 `raw-first-development.md`，正文分页能力已具备（pipeline parts + 夹具用例），
逐站 `pagination_selector` 待出现多页正文的来源时再登记，不以猜测配置。

## 第四十一轮核验（2026-09-13，恢复路径与条件请求，无页面采集）

目的：确认“预算停止后如何继续”与条件请求的实际行为，避免运行说明与实现不一致。

- 第 38 轮 IN-02 以 `deadline` 停止（`stop.unprocessed=2`）后执行 `crawl plan`：0 项——
  预算停止不入补抓队列。`manifests/failed_records.jsonl` 仅有 CN-04 一条失败及一条 `resolved` 处置行，
  故计划为空。结论：预算停止的继续方式是**重跑该来源 collect**（重新发现目标），
  `crawl plan`/`crawl resume` 只处理失败账中未解决的任务；运行说明据此修正（原文“下次可用 crawl plan/resume 继续”不准确）。
- 条件请求抽查：PIB 返回常量 `ETag: XXXXXXXX` 且 `cache-control: no-cache, no-store`；
  实测带 `If-None-Match: XXXXXXXX` 与不带均返回 200（服务端不据此 304），
  故该来源不会因占位 ETag 复用过期内容；进入状态库的 ETag 仅在非空时用于条件请求（实现未变）。

本日线上请求合计：168 + 3（本次 curl 抽查：1 次 HEAD 响应头 + 2 次状态码）= **171 个**（0 失败、0 绕过）。
