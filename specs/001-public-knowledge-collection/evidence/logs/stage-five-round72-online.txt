# 阶段五 第 72 轮（开发推进）：IN-02 队列批量推进（3 次连续运行）

## 登记（运行前写入，2026-09-14，Asia/Shanghai）

- 目的：IN-02 列表遍历已完成，队列剩 3813 个待处理目标（每目标约 2 个请求：详情 + 条约 PDF）。
  本轮连续运行 3 次、每次单轮上限 200，推进主要剩余缺口；发现层按增量核对（入口页无新增即停）。
- 窗口：`--start-date 2026-09-06`（包含式下界）。
- 预算：每次运行 ≤440/1800s，3 次合计 ≤1320（外层每次 +120s）；站点限速 0.5 req/s、并发 1 不变。
- 规则同前（robots、边界、Retry-After、不绕过、预算停止先保存进度）；按来源归属对账。

## 结果

| 运行 | run_id | 请求 | 结果要点 | 结束方式 |
| --- | --- | --- | --- | --- |
| 1 | `IN-02_20260914T002254` | 406 | 处理 200 篇条约（800 块）；pending 3813→**3613**；发现层增量核对 1 页 0 新增 | `ok`（830s） |
| 2 | `IN-02_20260914T003731` | 403 | 处理 200 篇（800 块）；pending 3613→**3413** | `ok`（805s） |
| 3 | `IN-02_20260914T005207` | 402 | 处理 200 篇（800 块）；pending 3413→**3213** | `ok`（803s） |

- 本轮合计 **1211 个请求**、新增文档 **600** 篇、块 **2400**；0 失败、0 跳过；
  三次运行的发现层均为增量核对（入口页 1 页、0 新增，未重取历史覆盖页）。
- 进度：IN-02 已处理 297→**897** / 4204 个已发现目标；剩余 3213 个待处理（详情 + 条约 PDF 约 2 请求/目标）。
