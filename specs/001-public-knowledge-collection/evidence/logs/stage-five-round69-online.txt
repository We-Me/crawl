# 阶段五 第 69 轮（开发推进）：验证已完成入口的增量核对并继续消费队列

## 登记（运行前写入，2026-09-13，Asia/Shanghai）

- 目的：验证第 68 轮修复（已完成入口不再整入口重取）在真实站点上的行为——IN-02 列表游标与
  CN-02 关键词“印度”检索游标已恢复 `completed`，本轮应只核对入口页并在无新增时以
  `incremental_head_checked` 结束；同时继续消费队列（IN-02 4023、CN-02 39、IN-05 41 目标 + 107 附件）。
- 窗口：`--start-date 2026-09-06`（包含式下界）。
- 预算：IN-02 ≤130/900s、CN-02 ≤40/700s、IN-05 ≤16/600s（外层各 +120s），合计 ≤186。
- 规则同前（robots、0.5 req/s、并发 1、边界、Retry-After、不绕过、预算停止先保存进度）；
  3 个运行并行启动，按来源归属对账。

## 结果

| 运行 | run_id | 请求 | 结果要点 | 结束方式 |
| --- | --- | --- | --- | --- |
| IN-02 | `IN-02_20260913T234549` | 123 | 处理 60 篇条约详情（240 块）；pending 4023→**3963**；**发现层按增量核对：只取入口页、0 新增**（`incremental_head_checked`，complete=true） | `ok`（队列未耗尽） |
| CN-02 | `CN-02_20260913T234224` | 29 | 处理 12 篇（72 块）；pending 39→**27**；检索仍整入口重取 2 页（当轮为检索修复前的代码路径，见第 70 轮） | `ok`（队列未耗尽） |
| IN-05 | `IN-05_20260913T234156` | 16 | 列表 1 篇（40 块）+ 附件；其页面新增大量附件候选 → 附件待处理 107→**197**；**列表入口同样增量核对（1 页、0 新增）** | `request_budget` |

- 本轮合计 **168 个请求**、新增文档 **73** 篇、块 **352**；0 失败；IN-02/CN-02 `reconciliation_ok=True`。
- 修复验证（IN-02/IN-05）：发现层请求由“每轮 5 页重复页”降为 1 页，游标保留 `completed`
  与历史计数（IN-02 note 含“上次终点 pagination_control_missing…”）；队列照常消费（60 篇/1 篇）。
