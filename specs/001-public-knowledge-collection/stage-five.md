# 阶段五：raw 采集完整性补齐与开发验收

复核日期：2026-09-13。代码基线：61e34d8。本文件是当前唯一工作顺序入口；raw-first-development.md 保存用户已确认的范围与规则，continuation.md 保存执行历史。原 S5 编号保留，下面任务定义替换旧“正式全范围验收与发布”安排。此轮仅修改 SDD，没有实施下面列出的修复或重跑线上测试。

## 已确认范围与当前事实

用户已确认全部 18 来源、按站适配抽象、运行时起始时间、独立结构与 div 空行最小分块、raw 优先、开发阶段 Git 管理。无需再确认首批名单或分块 A/B；高级后处理和发布暂缓，不构成当前任务依赖。近期开发窗口与有限预算由 Agent 记录后执行，扩大为持续运行或无边界历史采集不在本轮目标内。

61e34d8 已有 DiscoveryStrategy、BlockSegmenter 与实现类、--start-date、失败恢复中的原时间范围，不重新搭建这些基础设施。NEXT-04/T012 已完成。历史日志记录 406 passed；逐站报告为 9 来源有限样本通过、9 来源受限。该四分类“9/0/9/0”只表示入口与样本状态，不能解释为 9 站功能全面完成或其余没有代码工作。

来源覆盖仍有缺口：多站 max_pages=1；IN-02 已知 433 页但仅验证显式页入口；CN-04 列表/检索未完整；部分运行禁用附件，IN-05 有未处理附件。18 来源清单继续有效，受限状态不关闭其开发任务。当前 Windows 副本无 data/、dist/，留存日志中的计数和产物属于历史 WSL 时点，不能当成本轮重新检查的结果。

## 本次代码复核发现（需要实现验证）

| 事实与定位 | 影响 | 承接工作 |
| --- | --- | --- |
| discover/discoverer.py 的 discover_list/search/sitemap/api 直接获取并解析响应；当前类与 HttpClient 无原件持久化回调，pipeline 的归档发生在后续目标采集等路径 | 发现页成功响应缺少统一的 raw/manifest 留存路径；日志或后来手动采同一 URL 不能替代该次响应归档 | S5-01，优先 |
| discover_list 第二页 FetchError 仅 warning 后 break，最终返回已发现目标；列表/API 的上限或单页结束并不证明发现遍历完成 | 截断、失败和真实无下一页可能混同，现有 ok/zero_results 不能证明覆盖 | S5-03，优先 |
| collect 用 targets[:max_items] 后才计算 planned；unprocessed 主要是已选主目标数，不能代表发现截断或全部附件待处理量 | 未处理计数不足以代表全流程覆盖；不能根据 unprocessed=0 判断来源完成 | S5-06，结合 S5-03/04 |
| 预算停止不进入现有失败 plan/resume 队列；重新 collect 从入口再发现 | 当前是重跑语义，不是精确续跑；必须确认多轮有限预算能否推进到未完成部分 | S5-06 |
| 多数站点附件只做了有限样本或显式关闭 | 下载能力与该窗口附件覆盖是不同结论 | S5-04 |

以上为静态代码核对，不是本轮复现测试结果。先用小型固定响应补可复现证据，再实施最小修复；不得为了验证同一问题连续重复线上请求。

## 工作项、边界和完成证据

| 编号 / 原任务 | 状态与目标 | 具体完成标准 |
| --- | --- | --- |
| S5-01 / T005—T007、T016 | 工程实施完成（2026-09-13，见 [证据](evidence/stage-five-raw-completeness.md)）：成功业务响应统一留存 | 列表、搜索、sitemap、发现 API 与详情/正文分页/附件成功响应均可核对原件与账本，先持久化后解析；解析失败仍保留。复用 RawStore/ManifestWriter，避免双重写账；无需把发现页伪造成 document。用固定响应验证字节哈希、原件存在、解析失败和重复请求身份 |
| S5-03 / T005、T026 | 工程实施完成（2026-09-13，见 [证据](evidence/stage-five-raw-completeness.md)）：按站列表/查询分页覆盖 | 优先已有可访问入口与 IN-02 真实页参数，兼顾 CN-04 允许通道。记录确切的终止原因：到站点末页、日期边界、页数/项目数上限、预算、请求失败或规则未命中；失败不能变零结果。跨页去重、循环检测、置顶及未知日期不得造成漏采。不要求每站实现不存在的搜索/分页方式 |
| S5-04 / T006、T016、T026 | 工程实施完成（2026-09-13，见 [证据](evidence/stage-five-raw-completeness.md)）：窗口内附件闭环 | 与母页关联的已发现附件分成功、失败、边界拒绝、规则排除和待处理；附件 filter 排除必须可解释，不借限幅宣称完整。覆盖同域/明确许可域、重复附件、部分失败与预算中断。只处理已声明正文附件范围，不全站搜集无关下载 |
| S5-06 / T015—T019、T026、T027 工程部分 | 工程实施完成（2026-09-13，见 [证据](evidence/stage-five-raw-completeness.md)）：继续执行与 raw 开发验收 | 先核实并修复多轮有限运行无法向后推进的问题，最小方式复用现有运行状态/任务机制；必要时增加持久化待处理项或游标，先登记结构差异与兼容方式，不直接另建数据库/队列框架。验证停在发现、正文或附件阶段后继续时无遗漏、无无限重采；失败任务与正常待处理任务不混写为同一种失败。汇总下面的覆盖表和 raw 门槛 |
| S5-02 / T004、T026 | 分因完成（2026-09-13，见 [证据](evidence/stage-five-s5-02-restricted.md)）：受限来源分因处理 | 已按下一节完成本轮可核实部分：IN-04/07/08/09 定位为本地代理链路（fake-IP `198.18.0.0/15` + TLS 被切断；WSL 时钟/CA 已排除）、IN-03 别名归属核实但新域 robots 502 按保守拒绝、CN-01 旧域为官方镜像（不扩边界，改用新域栏目）、CN-03 规则拒绝与 CN-05/06/07 robots 508 维持原结论。剩余需要代理分流与站点侧条件（Q12），相关来源受限不阻断其他来源，不绕过限制 |
| S5-05 / 后处理质量 | DEFERRED | 精细正文、OCR、近似去重等暂缓；保留现有可用实现与测试，不作为 S5-01/03/04/06 前置 |
| S5-07 / 发布运维 | DEFERRED | 用户已要求开发阶段 Git 管理；不构建发布平台、不要求交接审批，不阻塞工程提交 |

执行顺序：先 S5-01，再 S5-03 与 S5-04；S5-06 随遇到的中断/覆盖缺口实施，最后做一次对应候选的开发验收。S5-02 可在独立工作期间推进。上述顺序指工作依赖，不要求创建多个 Agent。不再另起 NEXT-05B/C 的重复验收清单；它们的当前 raw 部分映射 S5-06，正式总验收仍按原 T 任务保留。

## 受限来源：工程诊断与外部决定分开

- 明确 robots 禁止、条款拒绝、登录/验证码：停止对应路径。记录已有证据；需要许可时给出对象和允许方式需求，不把用户批准当作网站许可。没有条件变化不反复探测。
- TLS/证书或连接问题：目前 IN-04/07/08/09 的记录不足以证明网站禁止访问。先核对目标 WSL 时钟、CA、代理配置与错误链；与已有工具结果作一次针对性对照。只采用可验证的可信证书来源，不使用 verify=False 或 curl -k。需要系统安装或组织 CA 时说明具体原因与权限；不默认换 Python、停用证书校验或扩大采集。
- HTTP 508（CN-05/06/07）：记录 HTTP 现象，不能凭状态码认定唯一原因或永久禁止；已有多轮复现，无新线索先复用证据。查出可行动配置/站点信息后再有限验证。
- 域名别名（IN-03、CN-01 旧域）：在已选来源内可以核实官方重定向/站内链接、归属及对应规则。证据充分时按精确域名配置并留证；这属于 Q12 的工程落实，不要求再次选择 18 来源。归属不明、主体变化或涉及新访问许可时只暂停相关域名，不使用广泛通配。
- CN-04 等前端列表：页面公开声明且合法可用的接口可以调查和适配；没有静态链接不等于只能等用户决策。不得猜测私有端点或绕过已拒绝路径；已有允许替代入口先推进。

历史阶段五把 Q12/Q13 归属混置：Q12 是发现规则/别名及访问边界，Q13 是来源范围与运行窗口。18 来源与近期开发窗口已明确；这些编号不能重新成为泛化批准门槛。

## raw 门槛与覆盖表

每个有正文的成功响应应可核对 raw 路径与字节哈希。304 无新响应正文时引用既有实体/状态并明确记录，不伪造新原件；robots 等控制请求保留访问决策证据，与业务资源分母区分。发现页归档不强制生成 normalized 文档。

来源报告至少分开记录：入口/发现状态、已遍历范围与停止原因、已发现主目标、成功/失败/跳过/待处理主目标、附件对应计数、raw 留存校验、已产出文档块追溯。如增加运行指标字段，在 data-model 与 structure-comparison 登记；不改变原文基础 JSONL 的含义。

- 已取得业务响应的原件与账本必须完整可对账；正文处理失败不影响 raw 留存。
- 已产出 documents/blocks 100% 可追溯；零文档为不适用，不能空分母判通过。
- 仅已知集合可以给完整率；发现提前截断时全窗口总量是未知，不填成 0 或拿当前成功数作“全站分母”。
- 失败历史不清零删除；成功恢复保留前后关联。仍失败、仍受限或仍有待处理时，明确记录该窗口未完成。
- “有限样本可用”“声明窗口采集完成”“正式总验收通过”是三个状态。开发阶段目标是可验证推进的 raw 链路，不把窗口选择升级成无需上限的大运行。
- 9 来源的原状态仍作历史样本证据；补充上述能力维度后再更新当前状态，不用新增一列“0 未完成”掩盖分页与附件缺口。

## 文档与验证只保留必要闭环

每项按“具体缺口 → 最小实现 → 对应用例/有限真实证据 → 状态更新”结束。统一采集归档、分页状态和覆盖报告共享机制；站点仅保存差异规则，避免每站复制一套下载/日志/测试体系。不要重建已完成的抽象类、dummy 或 LibreOffice 能力。

复用 406 passed 历史证据，修改相关路径后运行定向用例；共同底层变更形成新候选时做一次必要回归。成功后推进下一交付，不能为了等待外部输入继续扩测试。实际环境限制导致 skip 时写清楚并在可用环境完成必要验证，skip 不等于通过。

线上之前登记具体近期起始日、来源、请求/项目/时间预算以及要解决的问题。不得沿用过期示例日期冒充近期；优先程序内部截止，若另用 shell timeout，应晚于内部截止并留出清理时间，不能先杀进程再宣称停止账本完整。预算耗尽后先处理进度保存与未处理项，再登记下一次必要运行，不连续重启相同配置消耗预算。

完成代码后同步 runbook、data-model/契约差异、逐站证据和 tasks 中对应状态；仅相关变更才修改其余文档。无须重新生成全部说明、构建 wheel 或部署。提交开发成果，说明未完成项；T019/T026/T027 不因本文更新或有限样本自动勾选。

## 实施记录（2026-09-13）

上文“本次代码复核发现”的四个 READY 项已按本节更新为工程实施完成，证据见
[阶段五 raw 完整性证据](evidence/stage-five-raw-completeness.md)；本记录不改变 AT 用例状态，
也不把 T019/T026/T027 或 18 来源状态改为正式通过。

- **S5-01 完成（工程）**：新增 `src/crawler/output/archive.py`，发现页/正文分页/正文接口/附件共用
  “先原件与账本、后解析”，固定响应验证字节哈希、原件存在、解析失败保留与重复请求身份。
- **S5-03 完成（工程）**：每个入口记录终止原因与 `complete`；失败/截断记为 `partial`，
  未翻到的页写入发现游标，下一轮从该页续接；循环与页数上限有独立用例。按站取值仍随各来源推进。
- **S5-04 完成（工程）**：附件状态区分成功/失败/边界拒绝/规则排除（扩展名与适配规则，可解释计数）/
  重复/待处理；robots 拒绝不写失败账；预算停止的附件带母文档关联入队并在下一轮续传。
- **S5-06 完成（工程）**：`manifests/pending_items.json` 与 `manifests/discovery_cursors.json`
  支撑多轮有限预算向前推进（先 pending、后 refresh）；`coverage` 口径进入报告与
  `logs/metrics.json`；同时修复“上一轮已耗尽预算继续约束新一轮”的缺陷（预算按次运行生效）。
- **S5-02 保持受限、无新探测**：本轮没有新的外部线索，按“没有条件变化不反复探测”复用
  [十八来源状态](evidence/t026-eighteen-sources.md) 与第 37 轮复核记录；需要的是用户侧工程条件
  或允许方式（Q12），不是继续请求。
- **验证**：新增 13 项离线夹具用例；相关既有用例同步到新契约；全量回归 419 passed
  （[日志](evidence/logs/stage-five-full-pytest.txt)），契约同步检查通过。
- **仍未完成**：正式业务验收（T019/T026/T027）、逐站真实来源的分页与附件推进、来源启用范围、
  后处理质量与发布；这些不因本轮工程实施自动通过。

## 实施记录（2026-09-13 第二轮：分页规则落地、附件失败记账与真实推进）

本轮按 stage-five 工作顺序与 DEV-012 执行 12 次有限线上运行（第 42—48 轮，合计 74 个请求
——此前所写“8 次/30 请求”与逐轮计数不符，已按日志结果表更正——0 绕过），
登记与逐轮结果见 [第 42 轮日志](evidence/logs/stage-five-round42-online.txt)，
离线分页复算见 [复算日志](evidence/logs/stage-five-round42-offline-pagination.txt)。
本记录不改变 AT 用例状态，也不把 T019/T026/T027 或 18 来源改为正式通过。

- **S5-03 续作**：新增按站分页规则落地点——`adapter.pagination_selector` 同时作用于**正文分页**
  （`parse_html` 只在配置时生效，未配置保持通用行为）；IN-02 增加
  `pagination_merge_entry_params`（站点控件缺 `PageSize/sortBy` 且路径小写，直接跟随返回
  `No Record Found` 空壳；按入口派生下一页后连续取回第 1/2/3 页，游标推进到 page=4）；
  CN-02 增加 `.page li:nth-child(3) a` 分页规则与 `max_pages: 5`，第 42/43 轮真实翻页到第 2 页。
- **S5-03 缺陷修复**：CN-04 `/zhengce/index.htm` 为双 `<html>`、正文在 `body` 之外，通用范围取不到目标
  时曾被记成 `zero_results`（假零结果）；改为按整文档兜底并在发现 note 显式记录后，第 48 轮该入口
  产出 19 个政策目标并采集 2 份文档（`publication_date` 2026-09-10/11 均 in_window）。
- **S5-04 续作**：IN-05 AGMUT 页 97 个附件分母按状态分开记账（第 42 轮下载 6、待处理 91；
  第 43 轮 `resumed=7`、待处理 84）；IN-02 详情页附件 3 个下载 + 14 项规则排除（站点级 footer PDF）
  分开统计。**新增缺陷修复**：附件流式读取超时（第 45 轮 MEA 实测）不再以原始库异常终止整次运行，
  转 `FetchError` 记失败/失败账，未尝试项仍记 pending 续传。
- **S5-06 续作**：第 43 轮验证游标+待处理队列跨轮推进（CN-02 检索页 1→2、IN-05 附件续传）；
  修复搜索游标按模板共享的缺陷（`discover_search` 游标身份改为渲染后的检索 URL，不同关键词不再互相续接）。
- **验证**：新增/更新用例 12 项（解析器分页规则 3、发现分页 3、分页覆盖 3、注册表 2、附件失败 2、
  含双向验证）；全量回归 **431 passed**（[日志](evidence/logs/stage-five-full-pytest.txt)），
  `tools/sync_contracts.py --check` 通过（`source-registry.schema.json` 增加
  `pagination_merge_entry_params`），SDD 文档校验 PASS。
- **仍未完成**：IN-02 仅真实推进到第 3 页（433 页）；CN-04 检索通道仍待 Q12 允许方式；
  IN-05 仍有 84 个待处理附件；其余受限来源状态未变；正式业务验收（T019/T026/T027）不因本轮通过。

## 实施记录（2026-09-13 第三轮：受限来源分因、别名核实与分页/附件续接）

本轮完成 S5-02 的受限来源分因（第 49—53 轮：5 个 HTTP 请求 + 2 次 TLS 握手，0 绕过、0 越界请求），
并按 S5-03/04/06 做第 54/55 轮有限线上推进（36 个请求，0 新失败以外的 1 个大 PDF 读取中断如实入账）。
证据：[S5-02 分因](evidence/stage-five-s5-02-restricted.md)、
[第 49](evidence/logs/stage-five-round49-in03-alias.txt)/[50](evidence/logs/stage-five-round50-tls-diagnostics.txt)/[51](evidence/logs/stage-five-round51-in03-primary.txt)/[53](evidence/logs/stage-five-round53-cn01-old-column.txt) 轮日志、
[第 54 轮日志](evidence/logs/stage-five-round54-online.txt)、
[第 55 轮日志](evidence/logs/stage-five-round55-in05-attachments.txt)。
本记录不改变 AT 用例状态，也不把 T019/T026/T027 或 18 来源改为正式通过。

- **S5-02 分因（完成本轮可核实部分）**：
  - IN-04/07/08/09：TLS 失败定位为**本地代理链路**（WSL 时钟/CA/代理变量已排除；目标域解析到
    fake-IP `198.18.0.0/15`；`ladakh.gov.in` CONNECTED 后无对端证书，`www.gov.cn` 对照链完整）
    → 具体输入是 Windows 侧代理分流（Q12），不再按“本机信任库缺失”表述；
  - IN-03：旧域 `www.indiacode.nic.in` 是官方迁移公告（跳转 `indiacode.gov.in`）；新域 robots.txt
    返回 502（按 `rules_for_unavailable` 5xx 保守拒绝）、根页为 SPA 壳 → 维持受限，
    需要站点侧 robots 可用或公开接口/许可；
  - CN-01 旧域 `fmprc.gov.cn`：官方镜像（同栏目列表与新域字节一致）→ **不扩边界**，
    改用新域栏目 `/eng/xw/zyxw/` 入口（第 53/54 轮验证）；
  - CN-03 规则拒绝、CN-05/06/07 robots 508：无新线索，复用第 37 轮证据，不重复探测。
- **S5-03 续作**：IN-02 在合并参数规则下真实取回第 4 页（游标 `page=5`；433 页中已连续验证第 1—4 页，
  `max_pages: 5` 将在下一轮达页数上限）；CN-02 检索真实取回第 3 页（游标 `nPageIndex_=4`）。
- **S5-04 续作**：IN-05 AGMUT 附件 pending 84→83→74（第 54/55 轮）；第 55 轮 1 个约 162 MB 的站点
  年报 PDF 读取中断，按 `request_error` 入失败账（未冒充成功），同轮其余 8 个附件成功。
- **S5-06 续作**：CN-04 队列 17→15（2 个窗口外目标逐条记原因 `before_start_date:2026-09-04/2026-08-25`，
  不作为失败）；CN-01 新入口建立独立队列（7 目标，pending 5）。
- **更正**：第 42—48 轮此前记“8 次运行/30 请求”，逐轮重算为 **12 次/74 请求**（含第 45 轮崩溃的
  4 个请求），T026 与本文件同步更正。
- **验证**：全量回归 **433 passed**；`crawl check`（开发数据根）：manifest=150、documents=77、
  blocks=2770、raw_files=131、失败账 3 行（新增 1 行为第 55 轮大 PDF 读取中断，历史 2 行保留）、
  追溯 100%。
- **仍未完成**：IN-02 433 页仅推进到第 4 页（游标 page=5）；CN-02 仅到第 3 页（游标 4）；
  IN-05 仍有 74 个待处理附件；CN-04 队列 15 项、CN-01 队列 5 项；受限来源需代理分流或站点条件；
  正式业务验收（T019/T026/T027）不因本轮通过。

## 实施记录（2026-09-13 第四轮：队列与游标再推进）

第 56/57 轮按同一批能力继续消耗已确认可访问的工作（各 37 个请求，合计 74；0 失败、0 绕过），
登记与逐轮结果见 [第 56 轮日志](evidence/logs/stage-five-round56-online.txt) 与
[第 57 轮日志](evidence/logs/stage-five-round57-online.txt)。本记录不改变 AT 用例状态。

- **IN-02**：连续取回第 5、6 页 → 游标 `page=7`（第 1—6 页为真实数据；`max_pages: 5` 是
  **单次运行**页数上限，游标跨轮保存）。每轮文档 2、附件 2（规则排除 8—10 项）。
- **CN-02**：取回第 4、5 页 → 游标 `nPageIndex_=6`；每轮文档 3、附件 5（第 57 轮 1 个附件待处理）。
- **CN-04**：第 56/57 轮各消费 2 个队列目标，均为窗口外（2026-08-20/08-17/08-13），逐条记原因，
  pending 15→13→11（当前队列均为窗口外，属真实边界而非失败）。
- **CN-01**：第 56/57 轮各采 2 篇（`publication_date` 2026-09-12），pending 5（4 refresh）。
- **IN-05**：第 56/57 轮各续传 9 个附件、0 失败，pending 74→65→56。
- **验证**：全量 **433 passed**（本轮无代码改动，沿用第 54/55 轮回归）；`crawl check`（第 57 轮后）：
  manifest=212、documents=91、blocks=2957、raw_files=184、失败账 3 行（历史 2 + 第 55 轮大 PDF 1）、
  追溯 100%；第 49—57 轮合计 118 个 HTTP 请求 + 2 次 TLS 握手（0 绕过、0 越界页面），
  本轮新增文档 14 篇、块 187、raw 原件 53。
- **仍未完成**：IN-02（433 页中第 1—6 页）、CN-02（第 1—5 页）、CN-04 队列 11、CN-01 队列 5、
  IN-05 附件 56；受限来源需代理分流或站点条件；T019/T026/T027 不自动勾选。

## 实施记录（2026-09-13 第五轮：既有来源窗口内复采）

第 58 轮对 CN-08、IN-01、IN-06、IN-10 各做一次 ≤6 请求的低预算窗口内复采（合计 20 个请求、0 失败），
登记与结果见 [第 58 轮日志](evidence/logs/stage-five-round58-online.txt)。本记录不改变 AT 用例状态。

- CN-08：栏目 3 目标（`end_of_pages` complete），采 1 篇/6 块；1 个 2026-09-05 目标窗口外跳过并记原因。
- IN-01：两入口 2 目标，采 1 篇/29 块；队列余 5（`request_budget` 停止）。
- IN-06：列表 15 目标（`end_of_pages` complete），采 1 篇/35 块；队列余 14（`request_budget`）。
- IN-10：首页 17 目标，采 2 篇/67 块；6 条站外链接按 `domain_not_allowed` 跳过并逐条记原因。
- **验证**：`crawl check`（第 58 轮后）：manifest=223、documents=96、blocks=3094、raw_files=193、
  失败账 3 行、追溯 100%。

## 实施记录（2026-09-13 第六轮：全部可访问来源各推进一轮）

第 59 轮对当前**全部可访问**来源各推进一次（9 个运行、58 个请求、0 失败），登记与结果见
[第 59 轮日志](evidence/logs/stage-five-round59-online.txt)。本记录不改变 AT 用例状态。

- IN-02：第 7 页真实取回 → 游标 `page=8`（433 页中已证第 1—7 页）；CN-02：第 6 页 → 游标
  `nPageIndex_=7`。
- CN-04：消费 2 个**窗口内**队列目标（`publication_date` 2026-09-10/09-09）产出 2 篇文档/36 块，
  pending 11→9（此前窗口外目标不阻塞后续窗口内目标）。
- CN-01 采 1 篇（2026-09-12）、CN-08 采 1 篇（2026-09-06，下界包含）、IN-06 采 2 篇（2026-09-13）、
  IN-10 采 1 篇、IN-01 采 1 篇；窗口外与站外跳过逐条记原因。
- IN-05：附件续传 9 个、0 失败，pending 56→47。
- **验证**：新增文档 13 篇/174 块；`crawl check`（第 59 轮后）：manifest=265、documents=109、
  blocks=3268、raw_files=228、失败账 3 行、追溯 100%；本轮无代码改动，沿用 433 passed 回归。
- **仍未完成**：IN-02 第 8 页起；CN-02 第 7 页起；IN-05 附件 47；CN-04 队列 9；CN-01 队列 3；
  IN-01/IN-06/IN-10 队列 7/12/13；CN-08 清零；受限来源需外部条件（见 S5-02 证据与 Q12）。

## 实施记录（2026-09-13 第七轮：提高单轮目标数、清理队列）

第 60 轮把单轮 `--max-items` 从 2 提到 3—6，优先消耗已发现队列（8 个运行、74 个请求、0 失败），
登记与结果见 [第 60 轮日志](evidence/logs/stage-five-round60-online.txt)。本记录不改变 AT 用例状态。

- IN-02 取回第 8 页（游标 `page=9`，单轮处理 5 篇）；CN-02 取回第 7 页（游标 `nPageIndex_=8`，5 篇）。
- CN-04 处理 3 个窗口内目标（54 块）、pending 9→4；CN-01 pending 3→2；IN-01 处理 5 个文稿 PDF、
  pending 7→2；IN-06 处理 4 篇（66 块）、pending 12→8；IN-10 处理 5 篇（167 块）、pending 13→8。
- IN-05 附件 47→38（单轮 9 个、0 失败）。
- **验证**：新增文档 22 篇/337 块；`crawl check`（第 60 轮后）：manifest=324、documents=131、
  blocks=3605、raw_files=281、失败账 3 行、追溯 100%；本轮无代码改动，沿用 433 passed 回归。
- **仍未完成**：IN-02 第 9 页起（游标）、CN-02 第 8 页起、IN-05 附件 38、CN-04 队列 4（+10 refresh）、
  CN-01 2（+8 refresh）、IN-01 2、IN-06 8、IN-10 8；受限来源需外部条件（S5-02 证据与 Q12）。

## 实施记录（2026-09-13 第八轮：队列清零与游标继续）

第 61 轮继续按单轮 3—6 目标推进（8 个运行、80 个请求、0 失败），登记与结果见
[第 61 轮日志](evidence/logs/stage-five-round61-online.txt)。本记录不改变 AT 用例状态。

- **CN-04、CN-01 队列清零**：CN-04 第 61 轮处理 5 个窗口内目标（90 块），pending 4→0；
  CN-01 处理 1 篇（42 块）+ 2 个窗口外跳过，pending 2→0。
- IN-02 第 9 页真实取回（游标 `page=10`，单轮 6 篇/24 块 + 6 附件）；CN-02 第 8 页
  （游标 `nPageIndex_=9`，6 篇/36 块 + 6 附件）。
- IN-05 附件 38→**29**；IN-01 2→1；IN-06 8→4（第 61 轮 109 块）；IN-10 8→3（132 块）。
- **验证**：新增文档 28 篇/462 块；`crawl check`（第 61 轮后）：manifest=390、documents=159、
  blocks=4067、raw_files=335、失败账 3 行、追溯 100%；本轮无代码改动，沿用 433 passed 回归。
- **仍未完成**：IN-02 第 10 页起（游标）、CN-02 第 9 页起、IN-05 附件 29、IN-06 4、IN-10 3、
  IN-01 1；CN-04/CN-01/CN-08 队列清零（增量按列表入口重发现）；受限来源需外部条件（S5-02 与 Q12）。

## 实施记录（2026-09-13 第九轮：队列/游标继续与并发写入缺陷修复）

第 62 轮继续按单轮 6—7 目标推进，并把 8 个运行**并行**执行以暴露共享数据根的真实并发问题
（8 个运行、约 78 个请求、0 来源失败），登记与结果见
[第 62 轮日志](evidence/logs/stage-five-round62-online.txt)。本记录不改变 AT 用例状态。

- **发现并修复真实缺陷（默认并行口径）**：并行运行共用 `data/manifests/pending_items.json` 时，
  固定临时名 `pending_items.json.tmp` 被互相覆盖，CN-08 的 `os.replace` 抛
  `FileNotFoundError` 崩溃（退出码 1）；同类固定临时名还出现在 `discovery_cursors.json`、
  `incremental_state.json`、`logs/metrics.json` 与整文件 JSONL 写入，“读-改-写”也无跨进程锁，
  会整段丢失其他运行的状态更新。修复：`crawler/output/atomic.py` 提供唯一临时名原子写入
  （`atomic_writer`/`atomic_write_text`/`atomic_write_json`）与 `file_lock`（flock + 线程锁），
  `pending.py`/`cursor.py`/`state.py` 的读-改-写整段持锁，`metrics.py`/`jsonl.py` 改用唯一临时名。
- **回归用例**：新增 `tests/test_concurrent_state.py`（并发标记 8 项全部保留、并发游标 8 条全部保留、
  原子写入无残留临时文件）；修复前用例以同样的 `FileNotFoundError` 失败，修复后通过。
  全量回归 **436 passed**（原 433 + 3 新）。
- **离线复核**：7 个有报告运行的 `coverage.queue_state` 与当前 `pending_items.json` 逐来源一致，
  CN-08 崩溃轮的状态行也已落盘（内容被竞争进程替换带入目标文件）；原件/账本/文档/块未受影响，
  仅该轮运行指标行与报告缺失。
- 游标与队列：IN-02 第 10—11 页（游标 `page=11`）、CN-02 第 9 页（游标 `nPageIndex_=10`）；
  IN-05 附件 29→**20**；IN-06 4→1；IN-10 3→1；IN-01 1→2（新发现入队）；
  CN-01/CN-08 按列表入口增量重发现（CN-08 新增 1 篇窗口内成文）。
- **验证**：新增文档 23 篇/302 块；`crawl check`（第 62 轮后）：manifest=451、documents=182、
  blocks=4369、raw_files=383、失败账 3 行、追溯 100%。
- **仍未完成**：IN-02 第 12 页起（游标）、CN-02 第 10 页起、IN-05 附件 20、IN-06 1、IN-10 1、
  IN-01 2；CN-04/CN-01/CN-08 队列清零（增量按列表入口重发现）；受限来源需外部条件（S5-02 与 Q12）。

## 实施记录（2026-09-13 第十轮：并发修复验证、按来源对账与队列/游标继续）

第 63 轮把 7 个来源（IN-02/CN-02/IN-05/IN-06/IN-10/IN-01/CN-08）**并行**执行，验证第 62 轮的
并发修复，并补跑 CN-02 检索游标（约 90 个请求、0 来源失败），登记与结果见
[第 63 轮日志](evidence/logs/stage-five-round63-online.txt)。本记录不改变 AT 用例状态。

- **并发修复验证**：7 个并行运行 0 崩溃、0 失败（第 62 轮的 `FileNotFoundError` 未复现），
  每个运行的 `coverage.queue_state` 与最终 `pending_items.json` 逐来源一致，`data/` 无残留
  `.tmp-*`；首次启动被工具进程组回收（登记补充已记录，仅 CN-08 归档 1 个发现页）后改用
  持久会话启动，并发口径不变。
- **顺带修复监控误报**：并行运行时按运行对账使用**全局**文件增量，把其他来源的追加算进本次
  运行，导致 `reconciliation_ok=False` 全部误报。修复：`output_stats(source_id=…)` 按来源归属
  计数（账本/文档/失败账按 `source_id`、块行按 `doc_id` 前缀、原件按 `raw/<source_id>/`），
  `count_rows(..., source_id=…)` 同步支持；回归用例见 `tests/test_monitor.py`。修复后 CN-02 与
  IN-05 两个并行运行对账均 `ok=True`。
- 推进：IN-02 第 12 页（游标 page=12，7 篇/28 块）；CN-02 先消费队列 7 篇、补跑带
  `--keyword 印度` 取回第 10 页（5 篇/30 块，游标 `nPageIndex_=11`）；IN-05 附件 20→12→**4**；
  IN-06 队列 2；IN-10 **清零**（2 篇/47 块）；IN-01 附件全部取完（**0**）；CN-08 补跑正常结束。
- **验证**：新增文档 23 篇/218 块；全量 **437 passed**（新增按来源对账用例）；
  `crawl check`（第 63 轮后）：manifest=524、documents=205、blocks=4587、raw_files=449、
  失败账 3 行、追溯 100%。
- **仍未完成**：IN-02 第 13 页起（433 页）；CN-02 第 11 页起；IN-05 附件 4；IN-06 2；
  CN-01/CN-08/IN-10/IN-01 队列 0（增量按入口重发现）；受限来源需外部条件（S5-02 与 Q12）。

## 实施记录（2026-09-13 第十一轮：并发口径继续推进队列与游标）

第 64 轮延续第 63 轮口径（4 个并行运行、48 个请求、0 失败），登记与结果见
[第 64 轮日志](evidence/logs/stage-five-round64-online.txt)。

- IN-02 第 13 页（游标 `page=13`，7 篇/28 块）；CN-02 第 11 页（游标 `nPageIndex_=12`，
  6 篇/36 块 + 1 附件）；IN-05 旧附件剩余 4 个取完，转入列表目标（2 篇/62 块），其页面新增
  18 个附件候选入队；IN-06 2 篇（33 块），队列余 1。
- 4 个并行运行 `reconciliation_ok=True`（按来源归属对账），队列计数与状态文件逐来源一致。
- **验证**：新增文档 17 篇/159 块；`crawl check`（第 64 轮后）：manifest=566、documents=222、
  blocks=4746、raw_files=488、失败账 3 行、追溯 100%；本轮无代码改动，沿用 437 passed 回归。
- **仍未完成**：IN-02 第 14 页起（433 页）；CN-02 第 12 页起；IN-05 18 附件 + 51 列表目标；
  IN-06 1；CN-01/CN-08/IN-10/IN-01 队列 0（增量按入口重发现）；受限来源需外部条件（S5-02 与 Q12）。

## 实施记录（2026-09-13 第十二轮：单轮上限提到 14、检索遍历到站点末页）

第 65 轮把单轮 `--max-items` 提到 12—14（6 个并行运行、92 个请求、0 失败），加快分页与附件
覆盖推进，登记与结果见 [第 65 轮日志](evidence/logs/stage-five-round65-online.txt)。

- IN-02 连续取回第 13—14 页（游标 `page=15`，12 篇/48 块）；CN-02 取回第 12—13 页
  （12 篇/72 块）并**离线复核确认检索遍历到站点末页**：第 12 页原件尾页链接指向 13、
  第 13 页“下一页/尾页”均指向自身且只有 9 条链接（13 页 × 10 − 1 = 129 目标，与游标一致）；
  该结论仅覆盖“关键词 印度”检索入口，不代表条约库全站。
- IN-05 转入列表目标（2 篇/41 块），其页面再新增 11 个附件候选（pending 附件 29）；
  IN-06 处理 4 篇（89 块）；IN-10 增量重发现后处理 2 篇（59 块）、6 条站外跳过；
  CN-04 重发现（新增 1、复查 5）处理 2 篇（36 块）、3 个窗口外跳过。
- **验证**：新增文档 34 篇/345 块；6 个并行运行 `reconciliation_ok=True`；`crawl check`
  （第 65 轮后）：manifest=645、documents=256、blocks=5091、raw_files=559、失败账 3 行、追溯 100%。
- **仍未完成**：IN-02 第 15 页起（433 页）；CN-02 剩余 64 个队列目标 + 3 附件（检索入口已遍历完）；
  IN-05 29 附件 + 49 列表目标；IN-06 1；IN-10 1；CN-01/CN-08/IN-01 队列 0（增量按入口重发现）；
  受限来源需外部条件（S5-02 与 Q12）。

## 实施记录（2026-09-13 第十三轮：分页遍历模式与 IN-02 全量列表覆盖）

新增分页覆盖能力并把 IN-02 的已知 433 页列表一次遍历完成（1 个 discover-only 运行、420 个请求、
0 失败），登记与结果见 [第 66 轮日志](evidence/logs/stage-five-round66-online.txt)。

- **代码**：`crawl collect --discover-only`（只遍历发现入口、目标全部入队，不处理正文；发现阶段不受
  `--max-items` 限制，仍受页数/请求/截止与 robots 限速约束，`coverage.processing.mode=discovery_only`
  显式记录）与 `--max-pages N`（本次运行页数上限，覆盖适配配置）。发现响应仍按 S5-01 先归档后解析；
  runbook 已同步。
- **用例**：翻页遍历 + 入队 + 不请求正文 + 下一轮按队列续接处理；CLI 校验（`--discover-only` 与
  `--url` 互斥、退出码 2）；全量回归 **439 passed**（437 + 2）。
- **IN-02 列表遍历完成**：从游标 `page=15` 连续取到第 433 页（419 页、419 个发现原件、4071 个新目标）；
  站点第 433 页自述 `Displaying Item 4321 To 4327 of 4327` 且无下一页控件；逐页原件复算
  433 页 4327 槽位、唯一详情 URL **4204**（重复槽位 123），与队列目标数完全一致 → 该列表入口
  已遍历完，剩余 4142 个待处理目标属“已发现未处理”，不是发现缺口。
- **验证**：`crawl check`（第 66 轮后）：manifest=1064、documents=256、blocks=5091、raw_files=978、
  失败账 3 行、追溯 100%。
- **仍未完成**：IN-02 4142 个待处理目标按轮次处理（含 1 个待处理附件）；CN-02 队列 64 目标 + 3 附件；
  IN-05 29 附件 + 49 列表目标；IN-06 1；IN-10 1；CN-01/CN-08/IN-01 队列 0（增量按入口重发现）；
  受限来源需外部条件（S5-02 与 Q12）。

## 实施记录（2026-09-13 第十四轮：提高单轮处理量消费已发现队列）

第 67 轮把 IN-02 单轮 `--max-items` 提到 60（5 个并行运行、189 个请求、0 失败），登记与结果见
[第 67 轮日志](evidence/logs/stage-five-round67-online.txt)。

- IN-02 列表遍历已完成，本轮只消费队列：59 篇条约详情 + 附件（236 块），pending 4142→**4083**、
  refresh 50（游标保持 `page=6` 续接、发现阶段不再翻页）。
- CN-02 处理 11 篇（66 块）+ 1 附件，pending 57→**53**；IN-05 列表目标 7 篇（351 块），
  其页面新增 44 个附件候选 → 待处理附件 29→**73**；IN-06 3 篇（61 块）、pending 1；
  IN-10 1 篇（58 块）、7 条站外跳过、pending 1。
- 5 个运行 0 失败、`reconciliation_ok=True`；逐运行 `queue_state` 与 `pending_items.json`
  按来源核对一致。
- **验证**：新增文档 81 篇（256→337）、块 772（5091→5863）；`crawl check`：manifest=1238、
  documents=337、blocks=5863、raw_files=1139、失败账 3 行、追溯 100%（本轮无代码改动，
  沿用第 66 轮 439 passed 回归）。
- **仍未完成**：IN-02 4083 个待处理目标 + 1 附件（发现已完成，按轮次处理）；CN-02 53 目标 + 4 附件；
  IN-05 42 列表目标 + 73 附件；IN-06 1；IN-10 1；CN-01/CN-08/CN-04/IN-01 队列 0（增量按入口重发现）；
  受限来源需外部条件（S5-02 与 Q12）。

## 实施记录（2026-09-13 第十五轮：逐来源覆盖报告工具）

新增只读工具 `tools/coverage_table.py`，把 S5-03/S5-04/S5-06 要求的逐来源报告（入口/发现、
遍历范围、终止或续接原因、主目标与附件计数、raw 留存、失败与待处理状态）从现有账本一次汇总，
减少每轮人工重算与口径漂移：

- 数据来源：`sources.yaml`（来源/启用/发现方式）、`discovery_cursors.json`、`pending_items.json`、
  `crawl_manifest.jsonl`、`normalized/documents.jsonl` 与 `blocks.jsonl`、`failed_records.jsonl`；
  只读、不联网、不改写账本；`enabled: false` 的 `DEMO` 占位来源不列入 18 来源表。
- 主游标按 `pages_fetched` 再 `targets_found` 取最大者，并显示入口与活动续接页（IN-02
  `→ 续接 page=6&PageSize=10&sortBy=sortby`）；终止原因取游标 `note` 的分类前缀，区分站点末页、
  页数上限、目标上限、分页控件缺失等（第 66 轮遍历完成后 IN-02 主游标为 440 页/4270 目标、状态活动）。
- 生成结果归档为 [逐来源覆盖表](evidence/stage-five-coverage-table.md)：18 来源逐行，合计与
  `crawl check` 核对一致（manifest=1238、documents=337、blocks=5863、失败账 3 行、追溯 100%；
  主目标处理 147 / 待处理 4180、附件待处理 77）。本表只作逐来源口径汇总，不替代字节级校验与正式验收；
  发现未完成的来源总量仍记未知。
- **仍未完成**：同第十四轮（IN-02 4083、CN-02 53+4、IN-05 42+73、IN-06 1、IN-10 1 的队列消费，
  以及受限来源所需外部条件）。

## 实施记录（2026-09-13 第十六轮：修复已完成入口的重复遍历，第 68—70 轮验证）

第 68 轮按提高后的单轮上限继续消费队列（5 个并行运行、191 个请求、0 失败、新增 81 篇/525 块），
登记与结果见 [第 68 轮日志](evidence/logs/stage-five-round68-online.txt)；该轮同时暴露并按 S5-06
修复了一个“反复从头消耗预算”的缺陷。

- **缺陷**：入口遍历完成（游标 `completed`）后，下一轮发现从入口整入口重取：IN-02（433 页、
  单轮上限 5 页）在第 67/68 轮重取第 1—10 页、CN-02 关键词“印度”重取 4 页，两轮 `queue added=0`
  （目标全部已登记）；同时把游标从 `completed` 改写成 `active`，丢掉“列表已遍历完”的记录。
- **修复（最小）**：`Discoverer` 接受 `known_target`（管线传入本轮开始前已登记的主目标 URL 集合）；
  游标 `completed` 且**历史遍历页数 > 本轮页数上限**时改为增量核对——从入口向后取页，遇到首个全为
  已登记目标的页即停，终止原因 `incremental_head_checked`（`complete=true`），不重取历史覆盖页、
  不把已登记目标再标 refresh、不累计覆盖页数，note 保留原终点（`…；上次终点 <原终止原因>`）。
  页数上限内可整入口复核的入口行为不变；需要完整重遍历时显式 `--max-pages`（含 discover-only）。
- **用例**：`tests/test_pagination_coverage.py` 新增 3 项（已完成列表入口只核对入口页、入口页有新增时
  继续到“首个全为已登记目标的页”、检索入口同样按增量核对续接）；全量 **442 passed**（439+3）。
  runbook（collect 语义与终止原因表）、游标模块说明、`structure-comparison.md` 与 IN-02 适配注释
  同步更新；未新增契约字段。
- **状态恢复（一次性，证据见第 65/66 轮与第 68 轮日志）**：IN-02 列表游标（445 页/4316 目标）与
  CN-02 检索游标（21 页/209 目标）按“已完成遍历”恢复 `completed`、`next_url=null`，保留累计计数并在
  note 写明原始终点与重复页来源；第 69 轮重取又把 CN-02 改回 active，按同一证据再次恢复。
- **第 69/70 轮真实验证**（[第 69 轮](evidence/logs/stage-five-round69-online.txt)、
  [第 70 轮](evidence/logs/stage-five-round70-online.txt)，168+60 个请求、0 失败）：
  IN-02 发现层只取入口页 1 页、0 新增（`incremental_head_checked`），同轮仍处理 60 篇条约（240 块）；
  IN-05 列表入口同样 1 页 0 新增；CN-02 检索入口修复后 30 请求处理 13 篇（78 块），检索只核对第 1 页。
  队列消费不受影响（IN-02 pending 4023→3963、CN-02 14→1）。
- **验证**：全量 **442 passed**；`crawl check`（第 70 轮后）：manifest=1644、documents=517、
  blocks=6896、raw_files=1529、失败账 3 行、追溯 100%（517/517 文档、6896/6896 块）；
  `sync_contracts.py --check` 与 `verify_sdd_documents.py` 通过。
- **仍未完成**：IN-02 3963 个待处理目标；CN-02 1 目标 + 1 附件；IN-05 40 列表目标 + 197 待处理附件
  （页面持续新增候选）；IN-06 1；IN-10 1；CN-01/CN-04/CN-08/IN-01 队列 0（入口增量重发现）；
  受限来源需外部条件（S5-02 与 Q12）。

## 实施记录（2026-09-14 第十七轮：提高单轮预算批量推进 IN-02 队列）

第 71/72 轮在增量核对生效后继续消费各来源队列，登记与结果见
[第 71 轮日志](evidence/logs/stage-five-round71-online.txt) 与
[第 72 轮日志](evidence/logs/stage-five-round72-online.txt)。

- 第 71 轮（5 个运行、345 个请求、0 失败、新增 164 篇/885 块）：IN-02 单轮提到 150
  （pending 3963→**3813**）；CN-02 目标队列清零（剩 1 附件）；IN-05 1 篇 + 9 个 PDF
  （页面又新增 108 个候选 → 待处理附件 296）；IN-06 3 篇；IN-10 队列清零（6 篇）。
- 第 72 轮（IN-02 连续 3 次运行、1211 个请求、0 失败、新增 600 篇/2400 块）：单轮 200，
  pending 3813→**3213**，已处理 297→**897**/4204；三次运行的发现层均为增量核对
  （入口页 1 页、0 新增、0 重复页）。
- 修复后的真实行为在两类路径上都得到验证：入口页无新增时只核对 1 页（IN-02/CN-02/IN-05），
  入口页有新增时继续核对到站点终点（IN-06）。
- **验证**：`crawl check`（第 72 轮后）：manifest=3175、documents=1281、blocks=10181、
  raw_files=3037、失败账 3 行、追溯 100%（1281/1281 文档、10181/10181 块）；
  [逐来源覆盖表](evidence/stage-five-coverage-table.md) 同步重新生成（处理 1010 / 待处理 3253）。
- **仍未完成**：IN-02 3213 个待处理目标（约 2 请求/目标，按轮次推进）；IN-05 39 列表目标 +
  297 待处理附件（每处理一页新增数十个候选，规模随页数增长）；CN-02 1 附件；IN-06 1；
  其余来源队列 0（入口增量核对）；受限来源需外部条件（S5-02 与 Q12）。

## 实施记录（2026-09-14 第十八轮：IN-02 单轮 300 批量推进与小队列复核）

第 73 轮把 IN-02 单轮上限提到 300（连续 3 次运行），并同轮复核其余来源队列与入口，
登记与结果见 [第 73 轮日志](evidence/logs/stage-five-round73-online.txt)。

- IN-02（3 次运行、1814 个请求、900 篇/3600 块）：pending 3213→**2313**，已处理 1797/4204；
  三次发现层均为增量核对（1 页、0 新增）；其中 1 个条约 PDF 读取超时按失败记录账
  （`KW13B1831.pdf`，`record_only`、无原件、不重试绕过），其余成果不受影响。
- 小队列与入口复核（8 个运行、103 个请求、18 篇/434 块、0 失败）：IN-05 3 篇（列表目标 36、
  待处理附件 325）；CN-02 3 篇（目标 0、1 个新附件待下轮）；IN-06 3 篇；IN-10 2 篇；
  CN-01/CN-04/CN-08/IN-01 队列 0，按入口增量核对复核（CN-04 首页入口 7 目标、3 篇窗口外只留原件）。
- **验证**：`crawl check`（第 73 轮后）：manifest=5058、documents=2199、blocks=14215、
  raw_files=4904、失败账 4 行（新增 1 条 IN-02 PDF 读取超时）、追溯 100%（2199/2199、14215/14215）；
  [逐来源覆盖表](evidence/stage-five-coverage-table.md) 重新生成（处理 1920 / 待处理 2351）。
- **仍未完成**：IN-02 2313 个待处理目标 + 1 条失败任务（`crawl plan` 可补抓）；IN-05 36 列表目标 +
  325 待处理附件；CN-02 1 附件；IN-06/IN-10 各 1；其余来源队列 0（入口增量核对）；
  受限来源需外部条件（S5-02 与 Q12）。

## 实施记录（2026-09-14 第十九轮：附件大小上限改判边界拒绝与第 74 轮批量推进）

第 74 轮继续 IN-02 单轮 300 批量推进（4 次运行）并复核小队列来源；登记与结果见
[第 74 轮日志](evidence/logs/stage-five-round74-online.txt)。

- 实现修正：附件超过声明大小上限（`Downloader` 默认 64 MiB）原按 `FetchError` 记失败并写失败账，
  现改判为确定性边界拒绝（`AttachmentBoundaryRejected`）：内联下载与待处理续传两条路径统一记
  `attachments.boundary_rejected`、跳过原因 `boundary_rejected:size_limit_exceeded:<bytes>`，
  不写失败账、不进重试、不留待处理项。触发来源是第 74 轮 IN-02 的 `JP11B0043.pdf`（超过上限）；
  该失败账行按追加口径保留不回改。离线回归：`tests/test_attachment_coverage.py`（5 项，含 2 条新用例）
  与 `tests/test_fetch.py` 大小上限用例；全量 **444 passed**。
- IN-02（4 次运行、2409 个请求、1200 篇/4800 块）：pending 2313→**1113**，已处理 2997/4204
  （另有到期复查 94）；四次发现层均为增量核对（1 页、0 新增）；除超限 PDF 外 0 失败。
- 小队列（4 个运行、74 个请求、11 篇/301 块、0 失败）：IN-05 2 篇 + 46 个附件（列表目标 34、
  附件待处理 360）；CN-02 3 篇（续传 1 个附件；目标 0、附件待处理 1）；IN-06 3 篇（pending 1）；
  IN-10 3 篇（队列清零，`ok`）。
- **验证**：`crawl check`（第 74 轮后）：manifest=7520、documents=3410、blocks=19316、
  raw_files=7353、失败账 5 行、追溯 100%（3410/3410、19316/19316）；
  [逐来源覆盖表](evidence/stage-five-coverage-table.md) 重新生成（处理 3128 / 待处理 1148）。
- **仍未完成**：IN-02 1113 个待处理目标（约 2 请求/目标）；IN-05 34 列表目标 + 360 待处理附件；
  CN-02 1 附件；IN-06 1；其余来源队列 0（入口增量核对）；受限来源需外部条件（S5-02 与 Q12）。

## 实施记录（2026-09-14 第二十轮：IN-02 队列接近清零与全来源小队列收尾）

第 75 轮继续 IN-02 单轮 300 批量推进（4 次运行）并收尾其余小队列；登记与结果见
[第 75 轮日志](evidence/logs/stage-five-round75-online.txt)。

- IN-02（4 次运行、2126 个请求、1057 篇/4228 块、0 失败）：待处理 1113→**56**，已处理 4054/4204
  （另有到期复查 94）；四次发现层均为增量核对（1 页、0 新增）。运行 1 因站点前半程响应慢
  （约 30 分钟仅 8 篇）在 2701s 按 `retry_wait`（退避等待超剩余时间）停止，进度已保存；
  运行 2—4 各 300 篇、均 `ok`（1203—1742s）。
- 小队列（5 个运行、104 个请求、8 篇/253 块、0 失败）：CN-02 续传最后 1 个附件——
  **附件队列清零（8/8/0）**；IN-05 两跑（首跑被 `--max-items 2` 截到 2 个候选，同轮重跑），
  共下载 84 个附件、列表目标 31 待处理、附件待处理 374（长尾仍在增长）；IN-06/IN-10 队列清零。
- **验证**：`crawl check`（第 75 轮后）：manifest=9730、documents=4475、blocks=23797、
  raw_files=9546、失败账 5 行、追溯 100%（4475/4475、23797/23797）；
  [逐来源覆盖表](evidence/stage-five-coverage-table.md) 重新生成（处理 4193 / 待处理 87）。
- **仍未完成**：IN-02 56 个待处理目标（下一轮可清零）+ 94 到期复查；IN-05 31 列表目标 +
  374 待处理附件（每处理一页新增数十个候选，长尾）；其余来源队列 0（入口增量核对）；
  受限来源需外部条件（S5-02 与 Q12）。

## 实施记录（2026-09-14 第二十一轮：IN-02 待处理目标清零与零队列来源复核）

第 76 轮用一次 `--max-items 160` 运行收尾 IN-02 队列，并复核零队列来源；登记与结果见
[第 76 轮日志](evidence/logs/stage-five-round76-online.txt)。

- IN-02（1 次运行、230 个请求、113 篇/452 块）：**待处理目标 56→0（清零）**，已处理 4167/4204，
  到期复查 94→37（本轮核对 57 个）；1 个条约 PDF（`YU48B1207.pdf`）读取超时按失败记录账
  （`record_only`、无原件）；余 1 个附件待下一轮续传。运行按请求预算停止（230/1800s），进度已保存。
- IN-05（1 次运行、120 个请求、4 篇/141 块）：发现 175 个新附件、下载 114 个；列表目标
  15 处理/27 待处理；附件队列 526 总数/435 待处理（长尾、随列表页处理继续增长）。
- 零队列来源复核（CN-01/CN-04/CN-08/IN-01，24 个请求）：CN-01 1 个复查目标窗口外跳过；
  CN-04 复查重取 7 篇窗口内文档 + 2 篇窗口外跳过（余 1 目标 + 4 复查）；CN-08/IN-01 0 新增。
- 观察（如实记录，不越范围处理）：CN-04 等无 304/条件请求支持的站点，复查重取同内容会按
  每次抓取身份再记文档行（现行范围不去重、不建版本；T014 版本机制存在但未接入 collect）。
  全库同 (URL, sha256) 组 31 个、多出 57 行（含本轮 7 行），作为 T014/Q05 候选证据保留。
- **验证**：`crawl check`（第 76 轮后）：manifest=10092、documents=4599、blocks=24516、
  raw_files=9897、失败账 6 行（本轮新增 IN-02 超时 1 条）、追溯 100%（4599/4599、24516/24516）；
  [逐来源覆盖表](evidence/stage-five-coverage-table.md) 重新生成（处理 4317 / 待处理 28）。
- **仍未完成**：IN-02 37 个到期复查 + 1 个待处理附件；IN-05 27 列表目标 + 435 待处理附件（长尾）；
  CN-04 1 目标 + 4 复查；其余来源队列 0（入口增量核对）；受限来源需外部条件（S5-02 与 Q12）。

## 实施记录（2026-09-14 第二十二轮：IN-02/CN-04 队列全清与 IN-05 长尾加量）

第 77 轮收尾 IN-02/CN-04 队列并给 IN-05 加量（300 请求）；登记与结果见
[第 77 轮日志](evidence/logs/stage-five-round77-online.txt)。

- IN-02（77 个请求、37 篇/148 块）：核对 37 个到期复查并续传最后 1 个附件；**队列全清**
  （目标 4204/4204 处理、0 待处理、0 复查；附件 2/2/0），`unprocessed=0`。剩余仅为 3 条失败账
  历史行（`record_only`，不重试）。
- CN-04（8 个请求、4 篇/72 块）：复查重取 4 篇、1 篇窗口外跳过；**队列全清**
  （12 处理、0 待处理、0 复查、8 跳过），`unprocessed=0`。
- IN-05（300 个请求、8 篇/385 块）：发现 594 个新附件、下载 290 个（重复 7）；列表目标
  23 处理/19 待处理；附件队列 830 总数/739 待处理（长尾，含 100 MB+ 年报，单轮有效下载量
  受带宽与 0.5 req/s 限速约束）。
- **验证**：`crawl check`（第 77 轮后）：manifest=10474、documents=4648、blocks=25121、
  raw_files=10274、失败账 6 行、追溯 100%（4648/4648、25121/25121）；
  [逐来源覆盖表](evidence/stage-five-coverage-table.md) 重新生成（处理 4366 / 待处理 19）。
- **仍未完成**：IN-05 19 列表目标 + 739 待处理附件（长尾）；其余来源队列 0（入口增量核对）；
  受限来源需外部条件（S5-02 与 Q12）。

## 实施记录（2026-09-14 第二十三轮：第 78—82 轮把全部可采集来源队列清零）

第 78—82 轮以“清零号”收尾：第 78 轮 IN-05 加量（600 请求，清列表目标队列）、第 79/80/82 轮
续传 IN-05 附件长尾（100 → 600 → 余量）、第 81 轮七来源增量复核；登记与结果见
[第 78](evidence/logs/stage-five-round78-online.txt)、[第 79](evidence/logs/stage-five-round79-online.txt)、
[第 80](evidence/logs/stage-five-round80-online.txt)、[第 81](evidence/logs/stage-five-round81-online.txt)、
[第 82](evidence/logs/stage-five-round82-online.txt) 轮日志。

- IN-05（5 轮合计 1181 个请求、38 篇/1072 块）：列表目标 19→0，附件待处理 739→**0**
  （第 82 轮全清：附件 907 总数/905 处理/1 失败/1 边界拒绝；含 1 个大 PDF 读取中断历史失败）。
  附件候选随复查页继续出现是站点形态（每页 70+ 个 PDF 链接），本轮先按其时队列清空收口；
  站点后续更新会经发现层重新入队。
- CN-02（第 79/80/82 轮）：目标 129/129 处理、附件 10/10/0，**队列全清**。
- IN-06（第 80 轮）：23/23 处理，**队列全清**；第 78/81 轮发现过新增窗口内文稿（5+2 篇）。
- 第七来源复核（第 78/81 轮）：CN-01/CN-04/CN-08/IN-01/IN-02/IN-10 入口增量核对 0 新增，
  队列 0；IN-10 复查条目窗口外按规则跳过。
- **里程碑**：9 个可采集来源（CN-01/CN-02/CN-04/CN-08/IN-01/IN-02/IN-05/IN-06/IN-10）
  待处理与复查队列全部清零；受限来源（IN-03/IN-04/IN-07/IN-08/IN-09、CN-03/CN-05/CN-06/CN-07）
  仍按 S5-02/Q12 证据等待外部条件，不绕过、不重试探测。
- 观察延续（第 76 轮记录）：无 304 支持的站点复查重取同内容按每次抓取身份再记文档行；
  全库同 (URL, sha256) 组继续增长（T014/Q05 候选证据，未删除/未合并）。
- **验证**：`crawl check`（第 82 轮后）：manifest=12134、documents=4728、blocks=26774、
  raw_files=11826、失败账 6 行、追溯 100%（4728/4728、26774/26774）；
  [逐来源覆盖表](evidence/stage-five-coverage-table.md) 重新生成（**处理 4446 / 待处理 0、
  附件待处理 0**）。
- **仍未完成 / 后续**：受限来源外部条件（S5-02 与 Q12）；来源更新后的增量发现按轮次继续
  （入口增量核对 + 新目标入队）；T019/T026/T027 验收项未自动勾选。

## 实施记录（2026-09-14 第二十四轮：补抓分类修复与补抓路径大小上限一致性，第 83 轮）

第 83 轮把失败的补抓收口：修复分类缺陷后消费余下 4 项未关闭失败，并修掉补抓路径
绕过附件大小上限的口径不一致；登记与结果见
[第 83 轮日志](evidence/logs/stage-five-round83-recovery.txt)。

- **分类缺陷修复**（运行前）：`http_status_of` 的旧正则把超时消息里的 `port=443` 当作 HTTP
  4xx，2 项瞬时失败被误判“永久 4xx”转人工；改为只识别 `HTTP <状态码>` 前缀，并在失败账新增
  结构化 `http_status` 字段（`FailureWriter.record` 与 6 个 `_record_failure` 调用点），
  分类不再依赖错误文案。修复后 `crawl plan` 4 项全部为 `refetch`。
- **补抓结果**：IN-02（`IN-02_20260914T075240`，4 请求）3 项补抓成功、
  IN-05（`IN-05_20260914T075240`，2 请求）1 项补抓成功；两次运行 `ok`、0 失败、0 待处理，
  沿用原窗口 `scope_start_dates=["2026-09-06"]`。新归档 4 个原件（sha256 逐件核对），
  其中 IN-02 条约 PDF 69 MiB、IN-05 年报 PDF 155 MiB。
- **口径一致性修复（本轮发现的根因）**：补抓路径（`_recover_refetch` → `_collect_target`）此前
  用 `http.get` 直取响应，未执行附件的 64 MiB 声明上限，与附件下载/待处理续传路径不一致。
  目标获取改为 `_fetch_target_response`：非 HTML 原件与 `Downloader` 共用新增的
  `iter_capped_chunks` 边读边判，超限抛 `AttachmentBoundaryRejected`，按
  `boundary_rejected:size_limit_exceeded:<bytes>` 跳过（不写失败账、不进重试、计入覆盖口径）；
  `_recover_refetch` 对边界拒绝按 `skip` 关闭失败记录；HTML 页面不受附件上限约束。
  两份已归档的超限原件按 raw 优先保留（不删除、不覆盖），作为修复前历史例外记录在案。
- **回归测试**：新增 3 项——补抓超限附件按 skip 关闭且不写失败账、补抓 HTML 页面不受上限影响、
  直达非 HTML 目标同口径；全量回归 **450 passed**。
- **验证（离线）**：`crawl plan` 共 0 项（失败账 10 行全部关闭）；
  `crawl check`：manifest=12138、documents=4728、blocks=26774、raw_files=11830、失败账 10 行、
  追溯 100%（4728/4728、26774/26774）；[逐来源覆盖表](evidence/stage-five-coverage-table.md)
  重新生成（处理 4446 / 待处理 0、附件待处理 0）。
- **仍未完成 / 后续**：受限来源外部条件（S5-02 与 Q12）不变；9 个可采集来源队列保持清零，
  后续按轮次做增量核对与周期性复查；T019/T026/T027 验收项未自动勾选。

## 实施记录（2026-09-14 第二十五轮：9 来源入口增量核对，第 84 轮）

第 84 轮对全部可采集来源做一次入口增量核对（第 83 轮后约 25 分钟），登记与结果见
[第 84 轮日志](evidence/logs/stage-five-round84-online.txt)。

- **结果**：10 个运行、24 个请求、0 篇、0 失败；9 个来源入口均为 `incremental_head_checked`
  （`complete=true`，第 1 页全部为已登记目标，不重取历史覆盖页），**0 新增窗口内内容**；
  队列保持 0 待处理。CN-02 首跑漏传 `--keyword`（按 `not_implemented` 0 请求，未发站点请求），
  带关键词补跑 0 新增。
- **raw 增长**：入口页复核归档 13 行（4 个新物理文件，其余为同内容再引用）。
- **验证**：`crawl check`：manifest=12151、documents=4728、blocks=26774、raw_files=11834、
  失败账 10 行、追溯 100%；[逐来源覆盖表](evidence/stage-five-coverage-table.md) 重新生成
  （处理 4446 / 待处理 0、附件待处理 0）。
- **结论**：可采集来源处于低更新强度时段，增量核对按预期工作；后续按同样登记方式周期性复核，
  受限来源仍按 S5-02/Q12 等外部条件。

## 实施记录（2026-09-14 第二十六轮：补抓处置回写待处理项，第 85 轮）

第 84 轮复核发现对账矛盾：补抓成功只关失败账、不回写队列，IN-05 的一个附件在
`pending_items.json` 中长期滞留 `failed`，覆盖表因此显示“附件失败 1”。本轮修复并订正，
证据见[第 85 轮日志](evidence/logs/stage-five-round85-offline-pending-reconcile.txt)。

- **修复**：`_recover_refetch` 接收 `TargetOutcome` 并新增 `_reconcile_pending_item`——补抓成功
  按 `processed`、robots/边界拒绝按 `skipped` 回写同一 URL 的待处理项（含
  `crawl_id/raw_path/sha256` 与 note，幂等）；已提交文档仍不回写。
- **同轮扩展**：重解析路径（`_recover_reparse`）按同一实现回写 processed/skipped。
- **回归测试**：新增 2 项（重取、重解析各 1 项：失败队列项 → 补抓成功 → 状态与账本对齐），
  扩展边界拒绝用例断言；全量回归 **452 passed**；全量对账审计 0 处矛盾。
- **数据订正**：既有滞留项按 manifest `IN-05_20260914_2150` 与失败账 `resolved` 行离线订正为
  `processed`（保留 `previous_state=failed` 与 attempts，note 说明来源，不静默改数）。
- **验证**：IN-05 附件队列 `905/1 失败` → **`906 处理/0 失败/1 边界拒绝`**；待处理项 5374 processed/
  18 skipped/0 其它，账本未关闭 0；`crawl check`
  manifest=12151、documents=4728、blocks=26774、raw_files=11834、失败账 10 行、追溯 100%；
  覆盖表重新生成。
- **仍未完成 / 后续**：受限来源外部条件（S5-02/Q12）不变；增量核对按轮次继续；
  T019/T026/T027 未自动勾选。

## 实施记录（2026-09-14 第二十七轮：覆盖表补 S5-04 附件记录列，第 86 轮）

逐来源覆盖表此前只有队列口径的附件列（`pending_items.json`），S5-04 的附件覆盖
（成功/边界拒绝/失败/待处理）没有逐来源视图。本轮补齐，证据见
[第 86 轮日志](evidence/logs/stage-five-round86-offline-coverage-attachments.txt)。

- `tools/coverage_table.py` 新增“附件记录 成功/边界拒绝/失败/待处理”列：按
  `documents[].attachments[].status` 逐来源汇总（与文档/块同一“每次抓取身份”计数，不去重）；
  原列改名“附件队列”（队列状态，含第 85 轮补抓回写），两列都保留并在表头说明口径差异。
- 重算结果（只读、不联网）：附件队列 5374/0/0；附件记录 5815 成功/2 边界拒绝/3 失败/929 待处理
  （早期文档中的 pending 为生产时点快照，后续处置见队列列与逐轮日志）。
- 其余列与 `crawl check` 一致：处理 4446 / 待处理 0、manifest=12151、documents=4728、
  blocks=26774、失败账 10 行；未改数据、未改采集行为。

## 实施记录（2026-09-14 第二十八轮：`crawl check` 集成队列对账，第 87 轮）

第 85 轮的队列/失败账一致性修复此前只有一次性审计；本轮把它变成可复跑的交付校验，
证据见[第 87 轮日志](evidence/logs/stage-five-round87-offline-check-reconcile.txt)。

- 新增 `validate/reconcile.py`：待处理项 `failed` 而失败账该 URL 已按 `recovered`/`skip`
  关闭判为矛盾（其余组合只计数）；`crawl check` 文本输出“队列对账”一行、`--json` 增加
  `reconcile` 段并计入总 `ok`。
- 真实数据根结果：队列对账通过（待处理项 5392：processed 5374 / skipped 18；失败账未关闭 0）；
  其余交付校验计数不变；新增 5 项用例，全量回归 **457 passed**。

## 实施记录（2026-09-14 第二十九轮：交付清单计数与锁文件增量核验，第 88 轮）

NEXT-09 工程交接清单的计数停留在 2026-09-11 基线；阶段五推进后多处数字已过期。本轮只读复核，
按“增量不重写基线”的既有约定在清单末尾补照，证据见
[第 88 轮日志](evidence/logs/stage-five-round88-offline-inventory-counts.txt)。

- **锁文件未变**：`uv.lock` 74526 字节、SHA-256 `f5e8fa9c…` 与基线登记一致（未重跑 `uv sync`）。
- **计数更新**：跟踪文件 238→**335**；证据文件 47→**98**；`src/crawler` 63→**71** 个 `.py`、
  约 9510→**13042** 行；`tests` 24→**33**；`tools` 6→**9**（新增 `coverage_table.py`、
  `make_legacy_fixtures.py`、`offline_replay.py`）；`sources.yaml` 行更新为“18 个开发来源登记、
  DEMO 禁用”（正式启用仍待 Q12/Q13）。
- **边界**：未重做交接口径清单、未改代码/数据/用例状态；第 1/2 节 2026-09-11 基线表保持原样，
  当前实施状态以本文与 continuation.md 为准。
- **验证**：`sync_contracts.py --check` 契约 6 份一致、`verify_sdd_documents.py` PASS；
  T019/T026/T027 不自动勾选。

## 实施记录（2026-09-14 第三十轮：9 来源入口增量复核第二轮，第 89 轮）

距第 84 轮入口核对约 1 小时，按同一登记方式再跑一轮入口增量核对（9 个运行、26 个请求），
登记与结果见 [第 89 轮日志](evidence/logs/stage-five-round89-online.txt)。

- **结果**：全部 9 个来源入口 `incremental_head_checked`（`complete=true`，第 1 页均为已登记
  目标），**0 新增、0 失败、0 新失败账**；队列保持 0 待处理（IN-10 6 条跨域链接按
  `domain_not_allowed` 跳过，IN-05 `robots.txt` 404 按无规则处理，均为既有行为）。
- **raw 增长**：manifest 12151→12164（+13 行，4 个新物理文件；CN-01 游标页 11→12），
  raw_files 11834→11838。
- **验证**：`crawl check`：manifest=12164、documents=4728、blocks=26774、raw_files=11838、
  失败账 10 行、追溯 100%、队列对账通过；覆盖表重新生成（合计与第 86 轮一致，仅 raw 行数更新）。
- **结论**：窗口内（2026-09-06 起）无新增内容；受限来源仍按 S5-02/Q12 等外部条件；
  T019/T026/T027 不自动勾选。
