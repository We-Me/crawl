# 文档一致性检查记录

## 最新复核：阶段二提交 4f07c6f

本轮检查源码、CLI、运行说明和历史执行日志，未运行 pytest、联网试点或安装组件。确认 NEXT-01/02 已完成、NEXT-03 工程试点已完成；历史日志为 325 passed。识别预算未统一执行、CN-08 正文混入相关阅读、随包契约依赖源码根，以及续作状态与运行命令漂移。新增 stage-three.md，默认 NEXT-06 → NEXT-07 → NEXT-08，保留 NEXT-04 环境阻塞与 NEXT-05 业务待决。此处为最新文档状态，下方“CLI 缺失”等判断属于阶段一历史记录，不代表当前实现。


## 最新复核：开发停留在测试阶段

本轮只核对源码、配置、任务和历史日志，未运行 pytest、构建环境或请求网站。发现正式 CLI 缺失、T012 实际系统组件验证未完成、T019 正式验收仍 blocked，以及泛化加固缺少停止条件。已新增 continuation.md 与 DEV-010—DEV-012，同步入口、任务、验收和授权说明；T012/T019 更正为部分完成，既有工程证据保留。历史 310 passed 并非本轮重测结论。下方日志和统计均为此前时点结果。


## 最新补充：uv 镜像环境模板

已新增 templates/uv/pyproject.toml、uv-template.md 和 DEV-009，默认清华镜像替换内置 PyPI，保留 Python 3.9 优先及成熟稳定依赖规则。附件仅作镜像配置参考，未引入其模型依赖。已同步开发入口、复制清单、技术选型、起步步骤及 T002/T003 验收要求。

本次检查通过：TOML 静态解析与镜像默认值、Python 版本约束及空依赖检查；24 份 Markdown、87 处本地链接、92 项 Schema 正反例、4 份原始业务输入指纹及生成脚本语法。未执行镜像下载、依赖解析或 Linux 完整环境构建；以下较早记录中的文件和链接数量保留为当时结果。

版本：0.1.0｜日期：2026-09-11｜状态：文档与虚构样例检查通过，业务实施基线待评审

本次已完成四份输入的文档编制及文档和契约样例核验。检查对象是 37 条需求、27 项待实施任务、37 项待执行验收用例、6 份候选 Schema 和虚构 JSONL 示例。业务系统尚未实现，真实采集和验收未执行；本记录不能作为软件验收报告。（2026-09-11 后续：采集范围 T003—T019 已在固定夹具上实现并测试，见 [tasks.md](tasks.md) 与 [evidence/T019-acceptance.md](evidence/T019-acceptance.md)；本段描述的是初始文档检查时点的状态。）

## 实际检查结果

| 检查 | 结果 | 检查方法 |
| --- | --- | --- |
| docs 输入文件与迁移记录 | 通过 | Word 字节与迁移前一致；审查 Markdown 仅更新链接，登记保存迁移前后指纹 |
| 原文共同要求覆盖 | 通过 | S3 的 C01—C17 全部映射到存在的需求 |
| 需求和用例对应 | 通过 | 37 个唯一需求编号、37 个唯一 AT 编号，规格、矩阵及验收章节一一对应 |
| 任务依赖与状态 | 通过 | 27 个唯一任务，显式编号依赖无环，所有任务保持未完成 |
| 待决事项引用 | 通过 | 17 个 Q 编号均存在；需求中的 Q 引用均可解析，未写为已解决 |
| Markdown 可读结构 | 通过 | 23 个 Markdown 文件（含 docs/ 中两份审查、开发入口、技术选型和项目起步说明）可按 UTF-8 读取，代码围栏成对，无替换字符；所有本地文件与章节链接可定位 |
| 原表必填层级 | 通过 | manifest/document/block/failure 的基础 required 集合核对；建议字段未被批量升为必填 |
| JSON Schema 与格式规则 | 通过 | 92 项正反例验证，使用 PowerShell Test-Json 加显式日期、带时区时间及 URI 检查 |
| 示例原件与引用 | 通过 | 2 个实际虚构原件哈希匹配；2 条账本、2 份文档、3 个块和 1 条失败记录可对账 |
| 示例关系与顺序 | 通过 | 块引用存在、order 从示例选定的 0 连续递增；全文与块一致；附件关联账本及文档，原件哈希一致 |
| 来源配置示例 | 通过 | example.invalid 来源为虚构条目，enabled=false，无实际网络请求 |

92 项契约验证包括：有效对象、逐项删除必填字段、搜索缺少/带有 keyword、结构表格缺少 text、空表格、段落缺少 text、成功文档空全文、partial 空抽取、路径越界、错误日期时间、不存在的日历日期和 URL 缺少主机。此计数与业务的 37 个 AT 用例不同。

本环境的 Test-Json 默认不会对 format 作严格断言，已在验证脚本中补充检查；同时使用 DateKind String，避免 PowerShell 自动解析时间后改变被检数据。该补充覆盖本套 Schema 使用的格式，不宣称验证脚本是通用 JSON Schema 实现。

## 内容一致性复核

已核对 S1 六项交付，特别保留正文中强制列出的 logs/；未把 block 改名成 chunk；未把 A—G 分类和 A1/A2/B/C 证据等级混用；保留正文附件原件留存与物理去重之间的待决关系；数据模型区分原字段、正文补充字段和设计扩展。

S2 的 A—G 组织要求、更新频率、词包及来源专属约束均有保存位置。18 个首批来源及第 15 节条目已登记；其他来源表仍保留在原文转录中。没有把原件记载的 2026-09-07 站点核验日期写成本次实测结果。

任务顺序已复核：最小 HTML 闭环在 T009 包含基础 blocks 写出，T010 再完善多格式块模型，因此 M2 的可追溯成果不依赖尚未执行的后续结构任务。

## 尚未解决的业务事项

所有 17 项 Q 当前仍为 OPEN，详见 [待决事项](clarifications.md)。其中阶段范围、抓取关联与哈希语义、元数据和结构数据缺省、质量阈值及来源范围直接影响当前实施基线；七类实体、内部导入和检索/设备的决策只影响对应条件范围。当前可以使用本套文档评审和准备样本，不能宣称所有生产设计已冻结。

未执行的事项包括真实业务网站访问、爬虫实现、PDF/OCR/Office 解析器验证、完整数据集质量抽样、关键词召回测试、领域实体生产、索引和设备部署。它们按 tasks.md 和 acceptance.md 继续追踪。

## 输入目录调整

四份输入已移至 docs/。同步修正来源登记、Word 转录回链、原需求审查的相对链接、总入口、交接指南、设计目录及生成/验证脚本；源登记增加项目相对 path 和 relocation_history，保留迁移前指纹。校验范围增加 docs/ 中两份审查文件的链接。业务需求、任务和契约规则未修改。

## 复制到新项目的验证

已将 docs/、.specify/、specs/、两个 verify_sdd_documents 校验脚本和 SDD文档说明.md 复制到项目之外、名称包含空格的临时目录。未复制初始生成脚本；在另一个工作目录调用复制后的校验脚本，验证通过：92 项契约正反例、54 处本地链接、四份输入指纹、37 条需求及任务/验收映射均有效。

已移除校验脚本中的本机 Python 绝对路径。默认从 PATH 调用 python，也可用 -PythonPath 指定；验证要求 PowerShell 7.5+、Python 3.9+，不需要额外 Python 包。本次验证使用当前 Windows 环境，没有声称在其他操作系统实际运行过。文档包可整体复制作为开发输入；17 项业务待决事项仍按相关实施范围处理。

## 重新运行检查

在项目根目录运行 `./tools/verify_sdd_documents.ps1`，环境和参数说明见 [交接指南](quickstart.md)。验证脚本只读，输出最新检查结果；如果文档、原件或样例更新，应重新运行并更新本记录，而不能沿用本次通过结论。

## Python 开发约束更新

根据用户补充指令，业务开发语言已明确为 Python，运行版本和框架/依赖分别在 tech-stack.md 登记为 TD-01/TD-02，仍为 OPEN。新增根目录 AGENTS.md 的六项开发约束，并同步项目原则、规格、计划、任务、交接指南、检查表、文档入口和初始生成脚本。复制新项目时必须包含 AGENTS.md。

更新后检查通过：92 项契约正反例、22 个 Markdown 文件、59 处本地链接及原有需求/任务/用例映射；四份业务输入指纹不变。初始生成脚本语法检查通过，未重跑覆盖现有文档。现有 17 项业务 Q 与新增两项技术待选记录分开统计；没有宣称 Python 版本或框架已经选定，也未安装业务依赖。

## uv 与 Python 3.9 优先策略更新

后续用户指令明确使用 uv，优先 Python 3.9。当前基线以本节及 tech-stack.md 为准：包管理工具不再待选，TD-01 保留 3.9 优先但完整环境尚未验证；框架和依赖组合仍待选。已同步 AGENTS.md、项目原则、规格、技术计划、任务、交接指南、检查表、总入口和初始模板。

开发规则要求先构建已选范围的完整运行/开发/解析环境，区分依赖或平台不兼容与临时网络、权限和缺失系统组件问题。有失败证据后按 3.9、3.10、3.11 等逐个次版本提高，满足完整环境即停止；同步记录 uv 版本、解释器补丁版本、项目声明、锁文件及验证结果。

本次更新检查通过：92 项契约正反例、22 个 Markdown 文件、59 处本地链接，原始业务文件指纹不变。uv 命令说明依据官方文档核对；未实际安装 uv/Python 3.9、创建业务 .venv、选择框架或生成业务锁文件，因此不能据此声称 Python 3.9 环境已构建成功。

## 成熟依赖与稳定版本约束更新

新增 DEV-007，明确依赖按当前需求保守增加、优先成熟项目的兼容正式稳定版本、默认排除预发布与浮动 Git 引用。tech-stack.md 补充 uv 的 prerelease = "disallow" 配置说明、针对具体包更新、传递依赖与锁文件差异检查，以及必要例外的记录方式。已同步项目原则、计划、任务、交接、检查表、入口和初始模板。

更新后 92 项契约正反例和 59 处本地链接检查通过，四份原始输入指纹不变，生成脚本语法通过。本次仅完善文档，未安装新业务依赖或执行锁文件升级；Python 3.9 优先策略保持有效。


## 项目起步与运行配置补充核验

新增 DEV-008、project-startup.md、.env.example 与 .gitignore，同步 src 计划路径、数据根语义、任务和验收。Q14 的目录部分已有用户依据，仓库及发布策略继续 OPEN。检查通过：92 项契约正反例、23 份 Markdown、74 处本地链接、四份业务输入指纹及原有需求映射。新增 CFG-01—CFG-09 均未实施或运行；没有创建业务环境或安装依赖。生成脚本仅作模板与语法维护，未执行覆盖文档。
