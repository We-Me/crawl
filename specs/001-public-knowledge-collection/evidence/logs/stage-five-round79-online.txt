# 阶段五 第 79 轮（开发推进）：IN-05 附件续传启动 + CN-02/IN-06 余量清零

## 登记（运行前写入，2026-09-14，Asia/Shanghai）

- 目的：IN-05 列表目标队列已清零（第 78 轮），本轮起候选以旧附件开头，给 600 请求预算续传
  积压附件（748 个待处理）；CN-02（1 附件 + 27 复查）与 IN-06（1 目标 + 7 复查）清零余量。
- 窗口：`--start-date 2026-09-06`（包含式下界）。
- 预算：IN-05 ≤600/5400s；CN-02 ≤20/600s；IN-06 ≤15/600s。合计 ≤635（外层各 +120s）。
- 规则同前（robots、0.5 req/s、并发 1、边界、Retry-After、不绕过、预算停止先保存进度）。

## 结果

### 结果（3 个运行，137 个请求、14 篇、191 块，0 失败）

| 运行 | run_id | 请求 | 结果要点 | 结束方式 |
| --- | --- | --- | --- | --- |
| IN-05 | `IN-05_20260914T070518` | 102 | **开始续传积压附件**：候选 100 项全部为旧附件——续传成功 99 个（`resumed=99`），1 个超 64 MiB 上限按边界拒绝（`boundary_rejected:size_limit_exceeded`，不写失败账）；附件队列 189 处理 / 648 待处理 | `ok`（204s；候选上限 100 用尽，队列仍有 648） |
| CN-02 | `CN-02_20260914T070217` | 20 | 8 篇（48 块，复查重取）；续传 1 个附件 + 新发现 10 个附件全部下载；目标复查 19 剩，附件余 1 | `request_budget` |
| IN-06 | `IN-06_20260914T070207` | 15 | 6 篇（143 块，含新增窗口内文稿）；队列余 1 目标 + 1 复查 | `request_budget` |

- IN-05 说明：列表目标清零后候选以旧附件开头，续传路径验证生效；`--max-items` 默认 100 截断候选，
  下一轮起用显式 `--max-items` 放大单轮附件消费量。边界拒绝在真实站点首次出现并按新口径处理
  （无失败账、不重试）。
