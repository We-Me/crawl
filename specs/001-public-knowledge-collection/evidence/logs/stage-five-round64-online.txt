# 阶段五 第 64 轮（开发推进）：队列与游标继续（并发运行）

## 登记（运行前写入，2026-09-13，Asia/Shanghai）

- 目的：沿第 63 轮口径继续消耗队列与游标：IN-02 `page=12`、CN-02 `nPageIndex_=11`
  （检索发现需带 `--keyword 印度`）、IN-05 剩余 4 个附件与 53 个新列表目标、IN-06 2 个目标。
- 窗口：`--start-date 2026-09-06`（包含式下界）。
- 预算：IN-02 ≤16/450s、CN-02 ≤16/400s、IN-05 ≤10/600s、IN-06 ≤6/240s（外层各 +60s）。合计 ≤48。
- 规则同前（robots、0.5 req/s、并发 1、边界、Retry-After、不绕过、预算停止先保存进度）；
  4 个运行并行启动，继续按来源归属对账。

## 结果

| 运行 | run_id | 请求 | 结果要点 | 结束方式 |
| --- | --- | --- | --- | --- |
| IN-02 | `IN-02_20260913T223431` | 16 | 从游标 `page=12` 取回第 12 页（发现 10 目标），处理 7 篇（28 块）→ 游标 `page=13`；pending 65（含 1 附件） | `ok`（发现 partial） |
| CN-02 | `CN-02_20260913T223430` | 16 | 从游标 `nPageIndex_=11` 取回第 11 页（发现 10 目标），处理 6 篇（36 块）+ 1 个附件 → 游标 `nPageIndex_=12`；pending 57 + 3 附件 | `request_budget` |
| IN-05 | `IN-05_20260913T223419` | 10 | 剩余 4 个旧附件取完（processed 90）；开始处理列表目标 2 篇（62 块），其页面新增 18 个附件候选入队；pending 51 目标 + 18 附件 | `request_budget` |
| IN-06 | `IN-06_20260913T223408` | 6 | 处理 3 项（2 篇成文/33 块）；pending 1 | `request_budget` |

- 4 个并行运行 0 失败、0 崩溃；`reconciliation_ok=True`（按来源归属对账在并发下成立）；
  每个运行的 `coverage.queue_state` 与最终 `pending_items.json` 逐来源一致。
- 本轮合计 48 个请求；新增文档 **17** 篇（205→222）、块 **159**（4587→4746）、账本行 524→566；
  (`crawl check`) manifest=566、documents=222、blocks=4746、raw_files=488、失败账 3 行、追溯 100%。
- 游标/队列：IN-02 `page=13`；CN-02 `nPageIndex_=12`；IN-05 附件 18（新增）；IN-06 1。
