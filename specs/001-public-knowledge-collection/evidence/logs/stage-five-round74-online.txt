# 阶段五 第 74 轮（开发推进）：IN-02 批量推进 + 小队列

## 登记（运行前写入，2026-09-14，Asia/Shanghai）

- 目的：IN-02 队列剩 2313 个待处理目标 + 1 条失败任务；本轮连续 4 次运行、每次上限 300；
  同轮推进 IN-05（36 列表目标 + 325 附件）、CN-02（1 附件）、IN-06（1）、IN-10（1）。
- 窗口：`--start-date 2026-09-06`（包含式下界）。
- 预算：IN-02 每次 ≤660/2700s ×4 = ≤2640；IN-05 ≤50/1200s；CN-02/IN-06/IN-10 各 ≤8/300s。
  合计 ≤2714（外层各 +120s）。
- 规则同前（robots、0.5 req/s、并发 1、边界、Retry-After、不绕过、预算停止先保存进度）。

## 结果

### 小队列（4 个运行，74 个请求、11 篇、301 块，0 失败）

| 运行 | run_id | 请求 | 结果要点 | 结束方式 |
| --- | --- | --- | --- | --- |
| IN-05 | `IN-05_20260914T021102` | 50 | 2 篇 + 46 个附件；列表增量核对 1 页 0 新增；列表目标 pending 36→**34**、附件待处理 **360**（候选随列表目标处理增长） | `request_budget` |
| CN-02 | `CN-02_20260914T020927` | 8 | 3 篇（18 块）；续传附件 1 个（`resumed=1`）；检索增量核对 1 页 0 新增；目标 pending 0、附件待处理仍 1（新入队附件下轮预算内处理） | `request_budget` |
| IN-06 | `IN-06_20260914T020927` | 8 | 3 篇（53 块）；列表发现 1 页 15 目标、站点终点；pending 1 | `request_budget` |
| IN-10 | `IN-10_20260914T020958` | 8 | 3 篇（117 块）、7 条跳过（含 6 条站外/边界）；队列清零，增量核对 1 页 | `ok` |

### IN-02 批量（4 次串行运行，2409 个请求、1200 篇、4800 块，1 条失败记录）

| 运行 | run_id | 请求 | 结果要点 | 结束方式 |
| --- | --- | --- | --- | --- |
| 1 | `IN-02_20260914T023111` | 604 | 300 篇（1200 块）、297 个附件；1 个条约 PDF（`JP11B0043.pdf`）超过 64 MiB 附件上限，按当时口径记失败；pending 2313→**2013** | `partial`（退出码 1，失败已入账） |
| 2 | `IN-02_20260914T025309` | 602 | 300 篇（1200 块）、299 个附件；pending 2013→**1713** | `ok`（约 1203s） |
| 3 | `IN-02_20260914T031523` | 601 | 300 篇（1200 块）、299 个附件；pending 1713→**1413** | `ok`（约 1217s） |
| 4 | `IN-02_20260914T033725` | 602 | 300 篇（1200 块）、299 个附件；pending 1413→**1113** | `ok`（约 1203s） |

- 四次运行发现层均为增量核对（1 页、0 新增；历史覆盖 445 页）；条约详情与附件按规则下载，
  规则排除（`adapter_pattern:.pdf` 等）仅计数不影响正文。
- 运行 1 的 `JP11B0043.pdf` 是本轮唯一失败：站点原件超过 64 MiB 声明上限。该情形属确定性
  边界拒绝而非站点故障，已修正实现——此后超限附件记 `boundary_rejected:size_limit_exceeded`，
  不写失败账、不重试（见本轮实现记录与 `tests/test_attachment_coverage.py`）。失败账按追加口径
  保留该历史行，不回改。
- 进度：IN-02 已处理 **2997 / 4204** 个已发现目标，剩余 **1113** 个待处理（另有 94 个到期刷新）。

## 本轮更正（第 74 轮后）

- 附件大小上限从「传输失败（FetchError，可记录失败账）」改判为「确定性边界拒绝」
  （`AttachmentBoundaryRejected`）：覆盖口径记 `attachments.boundary_rejected`，跳过原因
  `boundary_rejected:size_limit_exceeded:<bytes>`，不写失败账、不进重试、不留待处理项；
  内联下载与待处理续传两条路径一致。离线验证：`tests/test_attachment_coverage.py`（5 项）与
  `tests/test_fetch.py`（含大小上限边界拒绝）通过；全量 444 项通过。
